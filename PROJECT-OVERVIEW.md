# نظرة شاملة على مشروع Black Whale Academy

هاد ملف تشرح فيه طريقة عمل التطبيق بالكامل — مصمّم عشان تفتحه بمحادثة Claude Chat عادية (مو Claude Code) وتناقش فيه أفكار تطوير جديدة، بدون ما تحتاج وصول لكود المشروع. لما توصلوا لقرار، ارجع فيه لهون (Claude Code) للتنفيذ الفعلي.

---

## شو هو التطبيق

تطبيق ويب (Next.js) لإدارة أكاديمية رياضية للأطفال — بيغطي تسجيل الأطفال، الاشتراك بأنشطة رياضية متعددة (سباحة، كرة قدم، كاراتيه...)، تتبّع الحصص المدفوعة والمستخدمة، وتسجيل الحضور اليومي عبر مسح كود QR. البيانات محفوظة بقاعدة بيانات Postgres (مستضافة على Neon)، والتطبيق منشور على Vercel.

## الفئات الثلاث اللي بتستخدم التطبيق

كل فئة عندها رابط مختلف، وكل رابط مصمم يعطيها بالضبط الصلاحيات يلي بتحتاجها ومش أكتر:

### 1. الأهل — رابط `/register` (عام، بدون كلمة مرور)
- فورم تسجيل: اسم الطفل، رقم تواصل، صورة.
- يختاروا نشاط واحد أو أكتر (مو بس نشاط واحد)، ولكل نشاط يختاروا "باقة" (مثلاً 8 حصص/300 درهم أو 12 حصة/400 درهم).
- بعد التسجيل، بيتولّد كود QR فريد للطفل، وبيقدروا يحمّلوه كصورة (PNG) لمشاركته أو حفظه.
- هالصفحة مقصود إنها معزولة تماماً — ما فيها أي رابط رجوع لباقي التطبيق، والأهل ما بشوفوا أي معلومة إدارية.

### 2. المدربين — رابط `/attendance` (عام، بدون كلمة مرور)
- المدرب يختار "شو النشاط الشغال هلق" (لازم يختار، مو اختياري) — هاد بيحدد لأي نشاط رح ينسجل الحضور.
- بيفتح كاميرا الموبايل ويمسح أكواد QR الأطفال.
- كل مسح ناجح بيسمع المدرب صوت مختلف حسب الحالة:
  - **نجاح عادي**: صوت بيب حاد مزدوج.
  - **الطفل خلصت حصصه** (بس التسجيل ينحسب برضه، مش منع): 3 بيبات متوسطة متتالية.
  - **كود غير معروف أو الطفل مش مسجل بهالنشاط تحديداً**: طنّة واحدة طويلة منخفضة.
- في جدول تحت الكاميرا يعرض كل الأطفال المسجلين بهالنشاط، مع إمكانية تبديل حاضر/غايب **يدوياً** كمان (بدون مسح QR إطلاقاً — زر بالجدول نفسه).
- ما فيه أي وصول لصفحة الإدارة أو الأسعار أو بيانات الأطفال الكاملة.

### 3. الإدارة/الموظفين — رابط `/admin` (محمي بكلمة مرور مشتركة)
- **كلمة المرور** محفوظة بقاعدة البيانات نفسها (مش env variable على Vercel)، وبتقدر تغيّرها من جوا الصفحة نفسها (زر "🔑 تغيير كلمة المرور") بدون أي تدخل تقني.
- **إدارة الأنشطة**: إضافة/تعديل/حذف نشاط (اسم، إيموجي، اسم المدرب، المواعيد)، وإضافة أكتر من "باقة" حصص/سعر لنفس النشاط.
- **زر تعبئة تلقائية**: بيضيف 7 أنشطة جاهزة (سباحة، Baby Swimming، كرة قدم، كرة سلة، جمباز، كاراتيه، كيك بوكسينج) بمواعيدها وأسعارها الحقيقية بضغطة وحدة.
- **قائمة الأطفال**: كل طفل بصير قدك توسّع صفه لتشوف كل اشتراكاته (نشاط + كم حصة استخدم/باقي + المبلغ المدفوع)، وتقدر:
  - تجدّد اشتراك (إضافة حصص جديدة فوق الموجودة).
  - تلغي اشتراك بنشاط معيّن.
  - تضيف اشتراك بنشاط جديد.
  - **تسجّل حصص "تفقد يدوي"**: لو الطفل كان يتحضر ورقياً قبل ما تبلّشوا تستخدموا النظام، تقدر تحط عدد الحصص يلي حضرها يدوياً، وبتنضاف تلقائياً فوق أي حصص جديدة عبر مسح QR.
- **طباعة/تحميل بطاقة QR** لكل طفل (صورة PNG كاملة: صورة الطفل + الاسم + الكود).
- **زر "حذف كل البيانات"**: يمسح كل شي (أطفال، أنشطة، اشتراكات، حضور) نهائياً — محمي بكتابة نص تأكيد يدوي.

### 4. السجل — رابط `/history` (عام، بدون كلمة مرور)
- استعراض سجل أي يوم سابق، بشكل عام (كل الأنشطة) أو حسب نشاط معيّن.

## آلية تتبّع الحصص (أهم قاعدة بالنظام)

- كل اشتراك (طفل + نشاط) عنده "رصيد حصص" = عدد الحصص الكلي (من الباقة المشتراة) ناقص عدد الحصص المستخدمة.
- **عدد الحصص المستخدمة ما بينخزن كرقم منفصل أبداً** — دايماً بينحسب لحظياً من عدد أيام الحضور الفعلية المسجّلة + أي حصص يدوية أضافها الأدمن. هيك ما ممكن يصير خطأ/تضارب بالعداد أبداً.
- تجديد الاشتراك بيضيف على نفس السجل (مش بينشئ اشتراك جديد مكرر).
- إذا الطفل خلصت حصصه ومسح QR، النظام بيسجله حاضر برضه (مش رافض الدخول)، بس بيطلع تحذير واضح للمدرب إنه لازم يجدد الاشتراك.
- الحضور بينسجل لكل نشاط لحاله — يعني نفس الطفل ممكن يكون حاضر بالسباحة الساعة 4 وغايب بالكاراتيه الساعة 6 بنفس اليوم، كسجلين منفصلين تماماً.

## قواعد العمل (Business Rules)

- الطفل ما ينقدر ينسجل حضوره مرتين لنفس النشاط بنفس اليوم — المسح الثاني بس بيحدّث وقت التسجيل، ما بينشئ سجل مكرر.
- الطفل بيقدر يشترك بعدة أنشطة بنفس الوقت — ما في حد أقصى لعدد الاشتراكات.
- كل نشاط ممكن يكون عنده أكتر من باقة حصص/سعر (مثلاً 8 حصص أو 12 حصة لنفس النشاط).
- **حذف نشاط بيحذف معه كل الاشتراكات المرتبطة فيه وسجل حضورها بالكامل** (مش بس منع اشتراكات جديدة) — الطفل نفسه ما بينحذف، بس بيفقد اشتراكه بهاد النشاط تحديداً وتاريخ حضوره فيه.
- حذف طفل بيحذف كل اشتراكاته وسجل حضوره بكل الأنشطة.
- تجديد اشتراك = إضافة على نفس السجل الموجود، مش إنشاء سجل جديد.
- لا يوجد حد أقصى لعدد الأنشطة أو الاشتراكات لكل طفل.
- حضور الطفل ما بينمنع أبداً بسبب انتهاء الحصص — بس بيظهر تحذير.

## هيكل قاعدة البيانات (مبسّط، مش SQL)

```
Activities (الأنشطة)
- id, name, emoji, instructor_name, schedule_text

Activity_packages (باقات كل نشاط)
- id, activity_id, session_count, price

Children (الأطفال)
- id, full_name, parent_contact, photo_base64, qr_token

Enrollments (اشتراك طفل بنشاط)
- id, child_id, activity_id, package_id
- sessions_total (عدد الحصص الكلي المشترى)
- sessions_used_offset (حصص يدوية/ورقية أضافها الأدمن)
- price_paid

Activity_attendance (سجل حضور)
- id, enrollment_id, attendance_date, status (حاضر/غايب)

Admin_settings (إعدادات الإدارة)
- password (كلمة مرور صفحة الإدارة، صف واحد بس)
```

ملاحظة: "عدد الحصص المستخدمة" و"الرصيد المتبقي" مش أعمدة بالجدول — دايماً بينحسبوا من عدد صفوف الحضور الفعلية + sessions_used_offset.

## تدفق الاستخدام (Workflow)

```
ولي الأمر يفتح رابط التسجيل
        ↓
يعبّي بيانات الطفل ويختار نشاط/أنشطة + باقة لكل وحدة
        ↓
يتولّد كود QR فريد للطفل (قابل للتحميل كصورة)
        ↓
المدرب يفتح رابط الحضور، يختار النشاط الجاري، ويمسح QR
        ↓
النظام يسجّل الحضور لهاد النشاط تحديداً بهاليوم
        ↓
الإدارة تفتح صف الطفل وتشوف كم حصة استخدم وكم باقي
        ↓
إذا خلصت الحصص، الإدارة تجدّد الاشتراك (تضيف باقة جديدة)
```

## أشياء أساسية ما لازم تتغيّر إلا إذا طلب المستخدم صراحة

- المسح عبر QR هو الوسيلة الأساسية لتسجيل الحضور — بس موجود أيضاً تبديل يدوي حاضر/غايب من قائمة الحضور نفسها (بدون مسح)، وهاد مقصود ومطلوب يضل موجود.
- الحضور ما بينمنع عند انتهاء الاشتراك (تحذير بس، مش حظر).
- عدد الحصص المستخدمة ما بينخزن كعداد مباشر — دايماً محسوب من سجل الحضور + الحصص اليدوية.
- ما في تسجيل دخول للأهل — رابط التسجيل والحضور عامّين تماماً.
- الصور تبقى داخل قاعدة البيانات نفسها (base64)، ما في خدمة تخزين صور خارجية.
- كلمة مرور واحدة مشتركة لصفحة الإدارة (مش حسابات مستخدمين منفصلة).

## المبادئ المعمارية (Architecture Principles)

هاد القسم مو للمستخدمين — هو توجيه لأي AI بيناقش تطوير هالمشروع. أي اقتراح تطوير لازم يحترم:

- **لا تكرار للبيانات (Single Source of Truth)** — كل معلومة إلها مكان واحد بس تتخزن فيه.
- **الحسابات تُستنتج، ما تُخزن** — إذا قيمة ممكن تنحسب من بيانات موجودة أصلاً (متل عدد الحصص المستخدمة)، ما تقترح عمود/جدول جديد يخزنها كعداد منفصل.
- **الإضافة أفضل من التعديل** — لما تحتاج تغيير بالبنية، فضّل إضافة عمود/جدول جديد (زي `sessions_used_offset` و`admin_settings`) على تعديل جدول موجود أو كسر التوافق مع بيانات حالية.
- **الموبايل أولاً** — أي ميزة جديدة لازم تشتغل منيح على شاشة موبايل صغيرة قبل ما تفكر بالحاسوب.
- **تعقيد أقل مقابل فايدة أكبر** — لا تقترح حل معقد (خدمة خارجية، جدول جديد، طبقة إضافية) لمشكلة صغيرة يمكن حلها بشكل أبسط.

## الأولويات

**Priority A**
- سرعة الاستخدام بالنادي (خصوصاً وقت المسح).
- البساطة — أقل عدد ضغطات ممكن لإنجاز أي مهمة.

**Priority B**
- المظهر الجميل والمناسب للأطفال والرياضة.

**Priority C**
- ميزات ثانوية/تحسينات تجميلية.

## قيود المشروع (Constraints)

- المشروع صغير ومقصود يضل بسيط بالبنية.
- ما بدنا خدمات خارجية مدفوعة إضافية (لا معالج دفع، لا تخزين صور منفصل، لا نظام إشعارات SMS/واتساب حالياً).
- نفضّل الاعتماد على Next.js + قاعدة بيانات وحدة (Postgres/Neon) بس.
- نفضّل حلول تنقدر تنشر على Vercel بسهولة بدون بنية تحتية إضافية.

## خارطة الطريق (Roadmap)

**Phase 1 — ✓ منجز**
- التسجيل + اختيار الأنشطة
- توليد QR ومشاركته كصورة
- تسجيل الحضور بالمسح + يدوي
- تتبّع الحصص والاشتراكات
- حماية صفحة الإدارة بكلمة مرور

**Phase 2 — أفكار قيد النقاش**
- Dashboard/تقارير وإحصائيات (أكتر نشاط مطلوب، معدل الحضور، الإيرادات)
- إشعارات تلقائية (واتساب/SMS) لتذكير الأهل بانتهاء الاشتراك

**Phase 3 — أفكار بعيدة المدى**
- بوابة يشوف فيها الأهل حصص طفلهم بأنفسهم
- صلاحيات موظفين متعددة (حسابات منفصلة بدل كلمة مرور مشتركة)
- تطبيق موبايل مخصص

## فلسفة التصميم

واجهة المستخدم لازم تضل:
- مناسبة للاستخدام من الموبايل بالدرجة الأولى.
- أزرار كبيرة وواضحة.
- أقل عدد شاشات/خطوات ممكنة لإنجاز أي مهمة.
- مفهومة لموظف غير تقني بدون أي تدريب.

## أشياء موجودة بالكود بس ممكن تتناقشوا فيها كتطوير مستقبلي

- **ما في نظام دفع إلكتروني** — تسجيل "المبلغ المدفوع" يدوي بس (رقم يكتبه الأدمن)، ما في ربط ببوابة دفع حقيقية.
- **ما في صفحة للأهل يتابعوا فيها حصص طفلهم بأنفسهم** — هلق هاد الشي بس ظاهر للإدارة والمدربين.
- **كلمة مرور واحدة مشتركة لكل الموظفين** — ما في حسابات مستخدمين منفصلة أو صلاحيات متفاوتة.
- **ما في تنبيهات تلقائية** (واتساب/SMS/إيميل) لتذكير الأهل إنه اشتراك ولدهم قارب يخلص أو خلص فعلاً.
- **ما في تقارير/إحصائيات** — البيانات موجودة بالقاعدة بس ما في شاشة تلخّصها.
- **زر "حذف كل البيانات" قوي جداً وبدون نسخة احتياطية** — أي حذف نهائي وما ممكن يترجع.

## وحدة Freelancers (المدربون المستقلون)

هاد قسم كامل جديد يوثّق وحدة منفصلة تماماً عن النظام الأساسي أعلاه (الأنشطة/الأطفال/الحضور) — إضافية بالكامل، ما بتلمس ولا جدول أو صفحة من النظام الأساسي.

### 1. نظرة عامة

الفريلانسرز مدربون مستقلون (مش موظفين بالأكاديمية) بيستأجروا وقت مسبح لحصصهم الخاصة. الدورة الكاملة: **حجز موعد ← موافقة/رفض من الإدارة ← تسجيل حضور الأطفال بمسح QR ← إغلاق الجلسة (يدوي أو تلقائي) ← محاسبة تلقائية بالـledger**.

### 2. الجداول

- **`freelancers`** — بيانات المدرب (الاسم، رقم الجوال، الـPIN المشفّر، حالة القفل بعد محاولات فاشلة، نوع الدفع، مفعّل/معطّل).
- **`level_default_pricing`** — السعر الافتراضي لكل مستوى (Level 1 إلى Level 4)، قابل للتعديل من الإدارة.
- **`freelancer_pricing_overrides`** — سعر خاص لمدرب معيّن بمستوى معيّن، يتجاوز السعر الافتراضي إذا موجود.
- **`freelancer_sessions`** — طلبات حجز المواعيد (تاريخ، وقت، حالة: pending/approved/checked_in/closed/rejected).
- **`freelancer_session_level_counts`** — عدد الأطفال المطلوبين لكل مستوى ضمن جلسة معيّنة.
- **`session_qr_tokens`** — أكواد QR مؤقتة لكل طفل بجلسة، بحد أقصى بعدد الأطفال المطلوبين لكل مستوى.
- **`freelancer_ledger_entries`** — سجل مالي append-only لكل حركة (دفعة، تحصيل جلسة، غرامة غياب، عكس حركة).
- **`freelancer_payment_type_history`** — أرشيف تلقائي لأي تغيير بنوع دفع المدرب.
- **`freelancer_settings`** — إعدادات عامة للوحدة (مفتاح/قيمة)، مثلاً قيمة غرامة الغياب.
- **`freelancer_notifications`** — إشعارات تلقائية للمدرب و/أو الإدارة عند تغيّر حالة جلسة.
- **View: `freelancer_balances`** — الرصيد الحالي لكل مدرب، محسوب دايماً كـ`SUM(amount)` من الـledger (مش عمود مخزّن).

### 3. دوال قاعدة البيانات (SQL Functions)

| الدالة | الغرض | الأقفال (Locking) |
|---|---|---|
| `reset_freelancer_test_data()` | تصفير كل بيانات الوحدة التجريبية (المدربون، الجلسات، الـledger، الإشعارات...) بدون التسعير/الإعدادات العامة | بدون قفل صفوف — يستخدم `SET LOCAL` لتجاوز حماية append-only مؤقتاً بنفس المعاملة |
| `close_freelancer_session(session_id, closed_by)` | إغلاق جلسة: تحصيل سعر كل حضور فعلي، غرامة غياب لكل رمز غير مستخدم، تحديث حالة الجلسة لـ`closed` | يقفل صف الجلسة (`FOR UPDATE`) وصف المدرب (`FOR UPDATE`) |
| `create_freelancer_session_request(freelancer_id, date, time, level_counts)` | إنشاء طلب حجز جديد بحالة `pending` بعد التأكد من وجود سعر لكل مستوى مطلوب | بدون قفل صفوف |
| `scan_freelancer_qr_token(session_id, token)` | تسجيل مسح رمز QR: تحويله لـ`scanned`، وتحويل الجلسة لـ`checked_in` عند أول مسح | يقفل صف الرمز (`FOR UPDATE`) |
| `add_freelancer_ledger_entry(freelancer_id, entry_type, amount, note, reversed_entry_id)` | إضافة حركة مالية يدوية (دفعة أو عكس حركة) مع التحقق من عدم عكس نفس الحركة مرتين | يقفل صف المدرب (`FOR UPDATE`) |

### 4. الـAPI Endpoints الفعلية

**المصادقة (Auth)**

| Method | Path | الوصف |
|---|---|---|
| POST | `/api/freelancer/login` | دخول بـرقم الجوال + PIN، يرجّع كوكي جلسة موقّعة |
| POST | `/api/freelancer/logout` | مسح كوكي الجلسة |
| GET | `/api/freelancer/me` | بيانات المدرب الحالي من الجلسة |

**واجهة المدرب (Freelancer-facing)**

| Method | Path | الوصف |
|---|---|---|
| GET | `/api/freelancer/pricing` | سعر كل مستوى الفعلي لهذا المدرب (افتراضي أو خاص) |
| GET | `/api/freelancer/sessions` | كل جلسات هذا المدرب، فلترة اختيارية بالحالة |
| POST | `/api/freelancer/sessions` | إنشاء طلب حجز جديد |
| GET | `/api/freelancer/sessions/[id]` | تفاصيل جلسة وحدة + الأعداد المطلوبة + رموز QR |
| POST | `/api/freelancer/sessions/[id]/qr-tokens` | توليد رمز QR جديد لمستوى معيّن بالجلسة |
| POST | `/api/freelancer/sessions/[id]/scan` | تسجيل مسح رمز QR |
| POST | `/api/freelancer/sessions/[id]/close` | إغلاق الجلسة يدوياً |
| GET | `/api/freelancer/ledger` | كل الحركات المالية + الرصيد الحالي |
| GET | `/api/freelancer/notifications` | إشعارات هذا المدرب |
| PATCH | `/api/freelancer/notifications/[id]/read` | تعليم إشعار كمقروء |

**واجهة الإدارة (Admin-facing)**

| Method | Path | الوصف |
|---|---|---|
| GET | `/api/admin/freelancer-sessions` | كل جلسات كل المدربين، فلترة اختيارية بالحالة |
| PATCH | `/api/admin/freelancer-sessions/[id]` | الموافقة أو الرفض على جلسة `pending` |
| GET | `/api/admin/freelancers` | كل المدربين + الرصيد الحالي لكل واحد |
| POST | `/api/admin/freelancers` | إنشاء مدرب جديد |
| GET | `/api/admin/freelancers/[id]` | تفاصيل مدرب + أسعاره الخاصة + رصيده |
| PATCH | `/api/admin/freelancers/[id]` | تعديل نوع الدفع و/أو تفعيل/تعطيل الحساب |
| PATCH | `/api/admin/freelancers/[id]/reset-pin` | تصفير PIN المدرب (يصفّر أيضاً محاولات الدخول الفاشلة والقفل) |
| POST | `/api/admin/freelancers/[id]/ledger` | إضافة حركة مالية يدوية (دفعة أو عكس حركة) |
| POST | `/api/admin/freelancers/[id]/pricing-overrides` | إضافة/تعديل سعر خاص لمستوى معيّن |
| DELETE | `/api/admin/freelancers/[id]/pricing-overrides` | حذف سعر خاص لمستوى معيّن |
| GET | `/api/admin/level-pricing` | كل الأسعار الافتراضية للمستويات |
| PATCH | `/api/admin/level-pricing` | تعديل السعر الافتراضي لمستوى معيّن |
| PATCH | `/api/admin/freelancer-settings/[key]` | تعديل قيمة إعداد عام (مثلاً غرامة الغياب) |
| POST | `/api/admin/reset-freelancer-test-data` | تصفير كل بيانات الوحدة التجريبية |

**تلقائي (Automated)**

| Method | Path | الوصف |
|---|---|---|
| GET | `/api/cron/close-expired-freelancer-sessions` | إغلاق تلقائي لأي جلسة تجاوزت مدة صلاحيتها، محمي بـ`CRON_SECRET` |

### 5. آلية المصادقة

- كوكي جلسة موقّعة (`bwa_freelancer_session`) — التوقيع HMAC-SHA256 عبر Web Crypto API (مش مكتبة JWT خارجية، ومش Node `crypto` العادي لأنه Middleware بيشتغل على Edge Runtime).
- مدة الجلسة: ساعة وحدة بشكل افتراضي، أو 30 يوم لو اختار المدرب "تذكرني على هذا الجهاز".
- **بدون جدول جلسات منفصل بقاعدة البيانات** — الجلسة كلها داخل الكوكي نفسه (توقيع + تاريخ انتهاء)، ما في أي صف يمثّلها بالقاعدة.
- قفل الحساب: بعد 5 محاولات دخول فاشلة متتالية، يُقفل الحساب 30 دقيقة تلقائياً.

### 6. آلية الإغلاق التلقائي

- بما إنه Vercel Cron على خطة Hobby محدود بمرة وحدة يومياً بس (غير كافي لإغلاق جلسات خلال ساعات من انتهائها)، استُخدم **GitHub Actions** بدلاً منه: workflow يشتغل كل 10 دقائق ويستدعي endpoint الإغلاق التلقائي.
- الـendpoint نفسه محمي بمقارنة `Authorization: Bearer <CRON_SECRET>` — مش عبر middleware.js أو كوكي جلسة.

### 7. ملاحظات فنية مهمة

- **مشكلة حقيقية صارت واتحلّت**: `lib/db.js` بيمرّر `fetchOptions: { cache: 'no-store' }` لعميل Neon (`neon()`) عمداً. بدونها، طلبات `fetch` الداخلية لمكتبة `@neondatabase/serverless` كانت تنخزّن مؤقتاً بواسطة Next.js **رغم** وجود `export const dynamic = 'force-dynamic'` على مستوى الـroute نفسه — هاد سبب قراءة بيانات قديمة فعلياً لوحظت بـ`/api/freelancer/pricing` و`/api/freelancer/notifications` (نفس عائلة مشكلة الكاش يلي وثّقناها سابقاً بالنظام الأساسي). **أي ملف جديد يتصل بقاعدة البيانات مستقبلاً يجب يستخدم `lib/db.js` الموجود كما هو، ولا ينشئ عميل Neon منفصل بدون نفس الإعداد.**

### 8. حالة الواجهات (UI)

كل وحدة Freelancers أعلاه **Backend/API فقط** حالياً. ما في أي واجهة مستخدم فعلية للإدارة (كل التفاعل حتى الآن عبر استدعاءات API مباشرة)، وللمدرب لا يوجد سوى صفحتين: `/freelancer/login` (فورم الدخول) و`/freelancer` (صفحة مؤقتة/placeholder بترحيب باسم المدرب وزر خروج بس). لوحة تحكم فعلية للمدرب (حجز موعد، توليد QR، مسح، متابعة رصيد) ولوحة إدارة (موافقة/رفض، إدارة المدربين) لسا ما اتبنت.

---

## شو بدي من Claude أثناء النقاش

لا أريد كتابة كود بهالمحادثة. أريد منك:

- نقد الفكرة يلي رح أطرحها بصراحة.
- اقتراح مشاكل مستقبلية ممكن تصير.
- مقارنة بدائل مختلفة للحل.
- التفكير بتجربة المستخدم (خصوصاً للموظف غير التقني وبالنادي المزعج/الصاخب).
- اقتراح أفضل workflow لأي ميزة جديدة.
- التنبيه إذا كان اقتراح معيّن رح يأثر على الأداء أو قاعدة البيانات.
- **ما تقترح تعديلات تغيّر فلسفة النظام** (القسم أعلاه "أشياء أساسية ما لازم تتغيّر") **إلا إذا سألتك عنها صراحة**.

لما نوصل لقرار واضح، بارجع فيه لمحادثة Claude Code للتنفيذ الفعلي بالكود.
