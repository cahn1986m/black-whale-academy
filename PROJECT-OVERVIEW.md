# نظرة شاملة على مشروع Black Whale Academy

هاد ملف تشرح فيه طريقة عمل التطبيق بالكامل — مصمّم عشان تفتحه بمحادثة Claude Chat عادية (مو Claude Code) وتناقش فيه أفكار تطوير جديدة، بدون ما تحتاج وصول لكود المشروع. لما توصلوا لقرار، ارجع فيه لهون (Claude Code) للتنفيذ الفعلي.

---

## شو هو التطبيق

تطبيق ويب (Next.js) لإدارة أكاديمية رياضية للأطفال — بيغطي تسجيل المتدربين (بياناتهم الشخصية والطبية)، الاشتراك بأنشطة رياضية متعددة (سباحة، كرة قدم، كاراتيه...)، تتبّع الحصص المدفوعة والمستخدمة (بالعدد وبالوقت سوا)، جدولة خانات زمنية أسبوعية لكل نشاط، موافقة الإدارة على كل تسجيل جديد قبل ما يصير فعّال، وتسجيل الحضور اليومي عبر مسح كود QR. بالإضافة لوحدتين إدارة مدربين منفصلتين: **المدربين الداخليين** (رواتب/بالحصة + سجل مالي) و**المدربين المستقلين (Freelancers)** يلي بيستأجروا وقت مسبح. الواجهة كاملة بالعربي والإنجليزي (RTL/LTR حقيقي). البيانات محفوظة بقاعدة بيانات Postgres (مستضافة على Neon)، والتطبيق منشور على Vercel.

## الفئات اللي بتستخدم التطبيق

كل فئة عندها رابط مختلف، وكل رابط مصمم يعطيها بالضبط الصلاحيات يلي بتحتاجها ومش أكتر:

### 1. الأهل — رابط `/register` (عام، بدون كلمة مرور)
- **فورم متعدد الخطوات (4 خطوات)** — بدل سكرول واحد طويل: **المتدرب** (الاسم الكامل، تاريخ الميلاد، الجنسية، الجنس، الصورة، اختيار الأنشطة+الباقات) → **ولي الأمر** (الاسم الكامل، صلة القرابة، رقم التواصل، البريد الإلكتروني، العنوان اختياري) → **معلومات طبية** (هل عنده حالة صحية؟ نعم/لا + تفاصيل إلزامية لو "نعم"؛ نفس النمط لسؤال الأدوية) → **الموافقات** (موافقة الشروط والأحكام إلزامية، موافقة استخدام الصور تسويقياً اختيارية).
- كل التحقق (validation) يصير خطوة بخطوة قبل ما يقدر يكمّل للتالية، بالإضافة لتحقق مطابق تماماً على السيرفر (`POST /api/register`).
- يختاروا نشاط واحد أو أكتر (مو بس نشاط واحد)، ولكل نشاط يختاروا "باقة" (مثلاً 8 حصص/300 درهم أو 12 حصة/400 درهم).
- **كل تسجيل جديد من هالفورم بيصير بحالة "قيد المراجعة" تلقائياً** — لازم موافقة الإدارة قبل ما يقدر المتدرب يسجّل حضوره فعلياً (تفصيل كامل بقسم "workflow موافقة الإدارة" تحت). بعد التسجيل مباشرة، بيتولّد كود QR فريد للمتدرب وبيقدروا يحمّلوه كصورة (PNG) — الكود جاهز فوراً، بس المسح الفعلي مرفوض لحد ما توافق الإدارة.
- سؤال "هل عند المتدرب احتياجات خاصة؟" **مش موجود بهالفورم إطلاقاً** — هاد الحقل إداري بس (راجع البند التالي وقسم "علامة الاحتياجات الخاصة").
- هالصفحة مقصود إنها معزولة تماماً — ما فيها أي رابط رجوع لباقي التطبيق، والأهل ما بشوفوا أي معلومة إدارية.
- **هاي الصفحة (و`/register/special` تحت) هني الصفحتين الوحيدتين يلي ضلّوا عامتين بالكامل بدون كلمة مرور** طول المشروع.
- **التسمية**: كل نص ظاهر للمستخدم بيستخدم "متدرب" بدل "طفل" (بالتسجيل، الإدارة، الحضور، بطاقة QR) — تغيير تسمية ظاهرة بس، أسماء الجداول/الأعمدة بقاعدة البيانات (`children`, `child_id`...) ضلّت زي ما هي عمداً (تغيير داخلي بدون فايدة حقيقية).
- **أعلى الفورم مباشرة**: شريط ترحيبي (banner) بعنوان سطر واحد locale-aware ("شكراً لاختيارك أكاديمية الحوت الأسود" / "Thank you for choosing Black Whale Sports Academy") فوق خلفية بتدرّج كحلي↔أزرق كهربائي، مع فقاعات صغيرة صاعدة بحركة CSS بحتة (بدون JS ثقيل أو أي أصل خارجي) — هوية بصرية "Clean Sporty" مستقلة عن باقي ألوان الصفحة، تحترم `prefers-reduced-motion` وتشتغل صح بالاتجاهين RTL/LTR.

### 1ب. الأهل (تسجيل ذوي احتياجات خاصة) — رابط `/register/special` (عام، بدون كلمة مرور)
- **نفس فورم الـ4 خطوات بالضبط** (نفس الكود، عبر `POST /api/register` نفسه)، بفرقين اثنين بس:
  - `has_special_needs` مثبّت `true` تلقائياً (مش سؤال نعم/لا زي بقية النموذج).
  - حقل "تفاصيل الاحتياجات الخاصة" **ظاهر مباشرة وإلزامي** بالخطوة الثالثة (مش سؤال مشروط بإجابة "نعم" الأول).
- نفس مسار "قيد المراجعة" تماماً — التسجيل من هون كمان بيحتاج موافقة الإدارة قبل ما يصير فعّال.
- **رابط مستقل تماماً، بلا أي ربط أو ظهور داخل التطبيق نفسه** — يعني مش موجود جوا `/register` ولا `/admin` — الوصول الوحيد إله زر مخصص بالصفحة الرئيسية (`/`) بجانب باقي المربعات، بالإضافة لزر "📋 نسخ الرابط" منفصل جنبه (بينسخ رابط الإنتاج الكامل والثابت `https://.../register/special` مباشرة للحافظة، بدون ما يفتح الصفحة).
- عنوان تبويب المتصفح ووصف مشاركة الرابط (Open Graph) **مطابقين تماماً** لـ`/register` العادي — بدون أي ذكر لـ"احتياجات خاصة" — عشان لقطة شاشة أو مشاركة الرابط ما تكشف معلومة حساسة عن الطفل.

### 2. موظفي النادي — رابط `/staff` (محمي بكلمة مرور مشتركة خاصة بالموظفين، منفصلة عن كلمة مرور الإدارة)
- بعد الدخول، بوابة (`/staff`) فيها خيارين:
  - **📋 تفقد الأكاديمية** → يوديك لـ`/attendance` (تفقد المتدربين، تفصيل كامل بالبند التالي).
  - **🏊 تفقد الفريلانسرز** → يوديك لـ`/staff/freelancer-scan` (قائمة جلسات الفريلانسرز الجاهزة للمسح، وصفحة مسح مخصصة لكل جلسة).
- في صفحة تالتة منفصلة (`/staff/instructor-attendance`) لتسجيل حضور/غياب **المدربين الداخليين** أنفسهم يوماً بيوم (تفصيل بقسم "وحدة المدربين الداخليين" تحت) — مو ضمن بوابة `/staff` الرئيسية كخيار ظاهر، وصولها مباشر عبر الرابط لمن يعرفه.
- كلمة مرور الموظفين تتغيّر من `/admin` (زر "🔑 كلمة مرور الموظف")، مش من داخل `/staff` نفسها.

### 3. المدربين (تفقد المتدربين) — عبر `/attendance` (محمي، خلف دخول الموظف)
- المدرب/الموظف يختار "شو النشاط الشغال هلق" (لازم يختار، مو اختياري) — هاد بيحدد لأي نشاط رح ينسجل الحضور.
- بيفتح كاميرا الموبايل ويمسح أكواد QR المتدربين.
- كل مسح بيسمع صوت مختلف حسب الحالة (**4 فئات صوتية**):
  - **نجاح عادي**: صوت بيب حاد مزدوج.
  - **المتدرب خلصت حصصه** (بس التسجيل ينحسب برضه، مش منع): 3 بيبات متوسطة متتالية.
  - **المتدرب خلصت مدة اشتراكه** (`expiry_date`، مستقل عن عدد الحصص، بس التسجيل ينحسب برضه): نغمتين هابطتين، **له أولوية أعلى** من تحذير "خلصت الحصص" لو الاثنين صحيحين سوا.
  - **مرفوض بالكامل** (طنّة واحدة طويلة منخفضة): تشمل الآن أكتر من سبب — كود غير معروف، المتدرب مش مسجل بهالنشاط، **أو الاشتراك "قيد المراجعة"/ملغى/مرفوض، أو المتدرب مؤرشف بالكامل** (تفصيل الحالتين الأخيرتين بقسمي "workflow موافقة الإدارة" و"أرشفة/استرجاع المتدرب" تحت). كل حالة برسالة نصية مختلفة وواضحة، بس نفس الصوت.
- في جدول تحت الكاميرا يعرض كل المتدربين المسجلين بهالنشاط، مع إمكانية تبديل حاضر/غايب **يدوياً** كمان (بدون مسح QR — زر بالجدول نفسه)، وبيعرض تحت اسم المتدرب حالته (خلصت الحصص / خلصت المدة / قيد المراجعة / مؤرشف) لو تنطبق. المتدربين "قيد المراجعة" أو "مؤرشف" بيظهروا بشارة واضحة وبدون زر تبديل حاضر/غايب أصلاً — عشان الموظف يفهم فوراً ليش المسح مرفوض، بدل ما يختفوا من القائمة ويبدوا "غير معروفين".
- ما فيه أي وصول لصفحة الإدارة أو الأسعار أو بيانات المتدربين الكاملة.

### 4. الإدارة — رابط `/admin` (محمي بكلمة مرور مشتركة خاصة بالإدارة)
- **كلمة المرور** محفوظة بقاعدة البيانات نفسها (مش env variable على Vercel)، وبتقدر تغيّرها من جوا الصفحة نفسها (زر "🔑 تغيير كلمة المرور"). كلمة المرور الافتراضية `admin123` **اتغيّرت فعلياً**.
- **إدارة الأنشطة**: إضافة/تعديل/حذف نشاط (اسم، إيموجي، اسم المدرب — مربوط الآن بمدرب داخلي فعلي عبر قائمة منسدلة، المواعيد كنص وصفي)، وإضافة أكتر من "باقة" حصص/سعر لنفس النشاط (كل باقة فيها حقل اختياري "حصص/أسبوع").
- **الخانات الزمنية لكل نشاط** (جديد — راجع قسم "نظام الجدولة" تحت للتفصيل الكامل): إضافة/حذف خانات أسبوعية متكررة (يوم + ساعة بداية، مدتها ساعة ثابتة) مباشرة تحت باقات كل نشاط.
- **زر تعبئة تلقائية**: بيضيف 7 أنشطة جاهزة بمواعيدها وأسعارها الحقيقية بضغطة وحدة.
- **قائمة المتدربين — مقسومة لقسمين**:
  - **نشطين** (الافتراضي، مفتوح دايماً) — كل متدرب غير مؤرشف.
  - **مؤرشف (N)** — قسم منفصل تحته، **مطوي افتراضياً**، بعنوان يوضح العدد وسهم (chevron) بيفتحه لما يُضغط.
  - بكلا القسمين، كل متدرب بصير قدك توسّع صفه لتشوف/تعدّل: بياناته الكاملة (شارة "⚠️ بيانات ناقصة" لأي حقل NULL بالحقول الإلزامية-بالفورم-الجديد-لكن-قابلة-للفراغ-بقاعدة-البيانات؛ الحقول الطبية وحقل الاحتياجات الخاصة تحديداً بتعرض حالة ثالثة "⚠️ لم يُسأل بعد" لو NULL، مختلفة بصرياً عن "لا")، كل اشتراكاته، وأرشفة/استرجاع المتدرب (زر + سبب إلزامي — تفصيل كامل بقسم "أرشفة/استرجاع المتدرب" تحت).
  - **لكل اشتراك بصف المتدرب**: حالته (نشط / قيد المراجعة / ملغى / مرفوض / خلصت الحصص / خلصت المدة)، تجديد، تسجيل حصص "تفقد يدوي"، **إلغاء مع سبب** (لاشتراك نشط، منفصل عن حذف الاشتراك الكامل)، **الخانات الزمنية المعيّنة** (checkboxes لخانات النشاط + حقل "عدد الحصص الأسبوعية الفعلي" اليدوي — تفصيل كامل بقسم "نظام الجدولة")، وحذف الاشتراك الكامل.
- **التسجيلات الجديدة بانتظار الموافقة**: قسم مباشر بالصفحة الرئيسية للإدارة، **مجمّع حسب المتدرب** (مو حسب كل اشتراك لحاله — لو نفس المتدرب سجّل بعدة أنشطة بنفس الاستمارة، بتظهر كلها سوا تحت اسمه وحدة، وقرار الموافقة/الرفض واحد بيطبّق عليها كلها مرة وحدة، لأنه الهدف التحقق من مصداقية المتدرب نفسه مش مراجعة كل نشاط لحاله). زر رفض إلزامي بسبب نصي.
- **طلبات الفريلانسرز**: قسم مباشر بالصفحة الرئيسية، بيعرض كل الجلسات المعلّقة، تفاصيل الأعداد المطلوبة لكل مستوى، والتكلفة المتوقعة الإجمالية، مع زري موافقة/رفض.
- **روابط منفصلة من الصفحة الرئيسية للإدارة**: 👥 إدارة المدربين الداخليين (`/admin/instructors`)، 🏊 إدارة الفريلانسرز (`/admin/freelancers`)، 📊 التقارير (`/admin/reports`)، 🗓️ الجدول الأسبوعي (`/admin/schedule`) — تفصيل كل وحدة بقسمها الخاص تحت.
- **طباعة/تحميل بطاقة QR** لكل متدرب (صورة PNG: صورة المتدرب + الاسم + الكود) — **بدون أي إشارة لحالة الاحتياجات الخاصة عليها إطلاقاً**، حتى لو كانت `true` بقاعدة البيانات.
- **زر "حذف كل البيانات"**: يمسح كل شي (متدربين، أنشطة، اشتراكات، حضور) نهائياً — محمي بكتابة نص تأكيد يدوي.
- **زر "تصفير بيانات Freelancers التجريبية"**: يمسح فقط بيانات وحدة Freelancers، محمي بكلمة مرور تصفير منفصلة.

### 5. السجل — رابط `/history` (عام، بدون كلمة مرور)
- استعراض سجل أي يوم سابق، بشكل عام (كل الأنشطة) أو حسب نشاط معيّن.
- `GET /api/attendance` يضل عام تماماً (غير محمي) لأنه هاي الصفحة بتعتمد عليه.

## آلية تتبّع الحصص (أهم قاعدة بالنظام)

- كل اشتراك (متدرب + نشاط) عنده "رصيد حصص" = عدد الحصص الكلي (من الباقة المشتراة) ناقص عدد الحصص المستخدمة.
- **عدد الحصص المستخدمة ما بينخزن كرقم منفصل أبداً** — دايماً بينحسب لحظياً من عدد أيام الحضور الفعلية المسجّلة + أي حصص يدوية أضافها الأدمن. هيك ما ممكن يصير خطأ/تضارب بالعداد أبداً.
- تجديد الاشتراك بيضيف على نفس السجل (مش بينشئ اشتراك جديد مكرر).
- إذا المتدرب خلصت حصصه ومسح QR، النظام بيسجله حاضر برضه (مش رافض الدخول)، بس بيطلع تحذير واضح للمدرب إنه لازم يجدد الاشتراك. **هاد الاستثناء ("الحضور ما بينمنع") ما بينطبق على اشتراك مش نشط أصلاً أو متدرب مؤرشف — راجع القسمين التاليين.**
- الحضور بينسجل لكل نشاط لحاله — يعني نفس المتدرب ممكن يكون حاضر بالسباحة الساعة 4 وغايب بالكاراتيه الساعة 6 بنفس اليوم، كسجلين منفصلين تماماً.

## workflow موافقة الإدارة على التسجيلات — جديد

كل اشتراك (`enrollments`) عنده الآن حالة (`status`) من أربعة قيم ممكنة:

| الحالة | معناها | مين بيحطها |
|---|---|---|
| `pending_approval` | تسجيل جديد بانتظار مراجعة الإدارة | تلقائي لأي تسجيل عام عبر `/register` أو `/register/special` |
| `active` | فعّال، المتدرب يقدر يسجّل حضوره | تلقائي لأي اشتراك/تجديد تنشئه الإدارة نفسها من `/admin` (الإدارة موثوقة أصلاً، ما بتحتاج توافق على نفسها)، أو بعد موافقة الإدارة على تسجيل `pending_approval` |
| `cancelled` | كان نشط وانلغى بسبب | زر "إلغاء مع سبب" بصف المتدرب بـ`/admin`، سبب إلزامي |
| `rejected` | تسجيل جديد انرفض | زر الرفض بقسم "التسجيلات الجديدة بانتظار الموافقة"، سبب إلزامي |

- **موافقة/رفض قرار واحد لكل متدرب، بيطبّق على كل اشتراكاته المعلّقة سوا** (راجع "التسجيلات الجديدة بانتظار الموافقة" أعلاه).
- **الحظر الصريح لغير `active` بالحضور**: هاد **استثناء واعٍ ومقصود** عن قاعدة "الحضور ما بينمنع" الأساسية بالنظام. اشتراك بحالة `pending_approval`/`cancelled`/`rejected` بيرفض المسح **والتبديل اليدوي سوا** رفضاً كاملاً — ما بينسجل أي صف حضور إطلاقاً، برسالة مختلفة لكل حالة (مثلاً "قيد المراجعة — تواصل مع الإدارة"). هاد الحظر منفصل تماماً بالكود عن تحذيرات "خلصت الحصص"/"خلصت المدة" (يلي بتحذّر بس بتسمح)، عشان ما يلتبسوا مع بعض مستقبلاً.
- **حالة تقنية خاصة اتحسبلها**: لو اشتراك سابق لنفس المتدرب بنفس النشاط كان `rejected`/`cancelled`، وحاول يسجّل بنفس النشاط مرة تانية عبر رابط عام — النظام بيحوّل نفس الصف لـ`pending_approval` من جديد (بدل ما يحاول ينشئ صف مكرر ويتصادم مع قيد `UNIQUE(child_id, activity_id)`).
- إعادة تنشيط اشتراك (موافقة، أو تجديد/إضافة من `/admin`) بتصفّر `status_reason` تلقائياً.

## أرشفة/استرجاع المتدرب — جديد

- `children.archived_at` + `children.archived_reason` — أرشفة متدرب بالكامل (مش حذف) من صفه بـ`/admin`، بسبب نصي إلزامي. زر "استرجاع" بيرجعه لحالته الطبيعية.
- **الأرشفة حظر على مستوى المتدرب نفسه، مش على مستوى اشتراك معيّن** — بتمنع الحضور (مسح + تبديل يدوي) **لكل اشتراكاته بكل الأنشطة**، بغض النظر عن حالة كل اشتراك لحاله، برسالة مختلفة تماماً عن "قيد المراجعة" ("المتدرب مؤرشف — تواصل مع الإدارة"). **استثناء واعٍ ثاني ومنفصل** عن قاعدة "الحضور ما بينمنع"، بالكود مفصول بوضوح عن حظر `pending_approval` وعن تحذيرات خلصت الحصص/المدة.
- المتدرب المؤرشف **بيضل ظاهر** بجدول `/attendance` (مش بيختفي) بشارة "مؤرشف" وبدون زر تبديل — عشان الموظف يفهم سبب الرفض فوراً بدل ما يبدو "كود غير معروف".
- **القائمة الرئيسية بـ`/admin` مقسومة نشطين/مؤرشفين** (تفصيل بقسم "الإدارة" أعلاه). `GET /api/child-list` صار افتراضياً بيرجّع النشطين بس (`WHERE archived_at IS NULL`)؛ قسم "مؤرشف" بـ`/admin` بيطلب النسخة الكاملة عبر `?archived=true`. **أي استهلاك مستقبلي لهالـendpoint (بحث، قائمة منسدلة، إلخ) بيرث الاستثناء الافتراضي هاد تلقائياً**، إلا إذا طلب صراحة `?archived=true`.

## نظام الجدولة (الخانات الزمنية) — جديد

- **`activity_time_slots`**: خانة زمنية أسبوعية متكررة لنشاط معيّن — يوم بالأسبوع + ساعة بداية، **مدتها ساعة كاملة دايماً** (قيد CHECK بقاعدة البيانات). ساعات الدوام الفعلية المسموحة (كل يوم 4-10 مساءً، بالإضافة للسبت والأحد 10 صباحاً-1 ظهراً) **قرار سياسة بمستوى الفورم/الـAPI فقط، مش قيد بقاعدة البيانات** — عمداً، لأنها ممكن تتغيّر بأي وقت (موسم جديد، رمضان...) بدون الحاجة لـmigration.
- **`enrollment_slot_assignments`**: تعيين اشتراك معيّن (مش المتدرب عموماً) لخانة أو أكتر من خانات نفس النشاط — من صف الاشتراك نفسه بـ`/admin` (checkboxes).
- **`/admin/schedule`**: شبكة أسبوعية كاملة (الأيام أعمدة، الساعات صفوف)، كل خلية فيها "chip" لكل نشاط عنده خانة بهاليوم/الساعة مع عدد المسجلين، والضغط عليها بيوسّعها ليعرض أسماء المتدربين المعيّنين فعلياً.
- **`enrollments.sessions_per_week_override`**: رقم يدوي اختياري بمستوى الاشتراك نفسه، بيفرض "عدد الحصص الأسبوعية الفعلي" المستخدم بحساب `expiry_date`، **بأولوية أعلى من أي مصدر آخر**.
- **أولوية حساب "عدد الحصص الأسبوعية الفعلي"** (المستخدم بمعادلة `expiry_date`، راجع القسم التالي)، بالترتيب:
  1. `sessions_per_week_override` (لو محدد يدوياً على الاشتراك) — بيفوز دايماً لو موجود.
  2. **عدد الخانات الزمنية الفعلية المعيّنة** لهالاشتراك (لو `> 0`) — أدق من رقم الباقة العام لأنه بيعكس الجدول الحقيقي المسجّل فيه هالمتدرب تحديداً.
  3. `sessions_per_week` المسجّل على الباقة نفسها (السلوك الأصلي قبل هالميزة).
  4. لو ولا وحدة من الثلاثة موجودة → بلا حد زمني (`expiry_date = NULL`).
  - إعادة الحساب بتصير تلقائياً بأي تغيير بأي من المصادر التلاتة (اشتراك/تجديد، تعيين/إلغاء تعيين خانة، حذف خانة كاملة، تعديل الـoverride) — دالة مشتركة وحدة (`lib/expiryDate.js`) بدل ما يتكرر المنطق بكل مكان.

## انتهاء الاشتراك بالوقت أو بالحصص (أيهما أسبق)

بالإضافة لانتهاء الاشتراك بسبب استهلاك كل الحصص، في مسار ثاني مستقل تماماً: **انتهاء المدة الزمنية**، بناءً على "عدد الحصص الأسبوعية الفعلي" (راجع أولوية الحساب بقسم "نظام الجدولة" أعلاه — عمداً صار مصدره أدق من مجرد رقم الباقة الثابت).

- لو في "عدد حصص أسبوعية فعلي" محسوب (من أي من المصادر الثلاثة): عند أي اشتراك جديد أو تجديد أو تغيير جدول، بيتحسب ويتخزن `expiry_date = تاريخ_العملية + CEIL(الرصيد_المتبقي ÷ العدد_الفعلي) أسابيع`.
- التجديد دايماً بيحسب من **تاريخ التجديد نفسه**، مش من تاريخ الانتهاء القديم.
- لو **ما في** أي مصدر لعدد الحصص الأسبوعية: ما في حساب انتهاء بالمرة (`expiry_date = NULL`، بدون حد زمني).
- **الحالة الثلاثية** (نشط / خلصت الحصص / خلصت المدة) معروضة بصرياً بمكانين: جدول الحضور بـ`/attendance`، وصف المتدرب بـ`/admin`. لو الحالتين صحيحتين سوا، "خلصت المدة" بتاخد الأولوية بالعرض والصوت.
- المسح ما بينرفض بسبب أي من الحالتين — نفس فلسفة "خلصت الحصص" الأصلية (تحذير بس). **هاد مختلف تماماً عن حظر `pending_approval`/الأرشفة يلي فعلاً بيرفض المسح — راجع القسمين أعلاه.**

## علامة الاحتياجات الخاصة — جديد

- `children.has_special_needs` (بولياني) + `children.special_needs_details` (نص) — نفس نمط الحقول الطبية بالضبط: `NULL` = "محدّش سأل"، مختلف عن `false` = "لا صراحة".
- **إداري بالكامل** — ظاهر ومُعدَّل من صف المتدرب بـ`/admin` بس. **ما بيظهر إطلاقاً** بصفحة المسح (`/attendance`، لا بالكاميرا ولا بجدول التبديل اليدوي) ولا ببطاقة QR المطبوعة/المحمّلة — حتى لو كانت قيمته `true`.
- فورم `/register` العادي **ما بيسأل هالسؤال إطلاقاً** (يضل `NULL` لأي تسجيل عام عادي). الاستثناء الوحيد: `/register/special` (راجع قسم "1ب" أعلاه) بيثبّته `true` تلقائياً ويطلب التفاصيل مباشرة كجزء من التسجيل نفسه.

## سجل المدربين الماليين — نمط مشترك بين الداخليين والفريلانسرز

كلا نوعي المدربين (الداخليين والمستقلين) عندهم الآن **سبب تعطيل إلزامي** لما يتعطّل حسابهم — عمود `deactivation_reason` بجدولي `instructors` و`freelancers` سوا، بسبب نصي إلزامي بمستوى الفورم عند الضغط على "تعطيل الحساب"، ظاهر بصف المدرب طول ما الحساب معطّل. إعادة التفعيل بتصفّر السبب تلقائياً.

سجلاتهم المالية (`instructor_ledger_entries` و`freelancer_ledger_entries`) عندهم نفس البنية: `entry_type` (دفعة/عكس حركة)، مبلغ موقّع (موجب/سالب)، رصيد بعد الحركة، ملاحظة نصية حرة، وربط اختياري بحركة معكوسة. **دالة `add_instructor_ledger_entry()` كانت أصلاً بتدعم دفعة بسبب حر (نص + مبلغ بأي اتجاه) من زمان — كانت موجودة بس مش مكشوفة بواجهة `/admin/instructors`؛ صار الآن فيها فورم "➕ تسجيل دفعة" (اتجاه وارد/صادر + مبلغ + سبب حر) وقائمة حركات كاملة مع إمكانية عكس أي حركة، بنفس نمط `/admin/freelancers` تماماً.

## وحدة المدربين الداخليين (Instructors)

مدربين موظّفين فعلياً بالنادي (مش مستقلين زي الـFreelancers) — بيتحدد نوع أجرهم (`pay_type`): **بالحصة اليومية** (`default_rate_per_day`) أو **راتب شهري ثابت** (`monthly_salary` + `monthly_absence_deduction` لكل يوم غياب).

- **`/admin/instructors`**: إضافة مدرب جديد (اسم، معلومة تواصل، نوع الأجر وتفاصيله)، تفعيل/تعطيل الحساب (بسبب إلزامي، راجع القسم أعلاه)، تعديل بياناته، **إصدار راتب الشهر** (زر مباشر للمدربين براتب شهري)، والسجل المالي الكامل (تسجيل دفعة/خصم بسبب حر + عرض/عكس الحركات).
- كل نشاط بـ`/admin` بيرتبط بمدرب داخلي فعلي عبر قائمة منسدلة (`instructor_id`) بدل ما يكون بس اسم نصي حر.
- **`/staff/instructor-attendance`**: صفحة منفصلة يسجّل فيها الموظف حضور/غياب كل مدرب داخلي نشط، يوم بيوم. تسجيل الحضور/الغياب بيولّد حركة بالسجل المالي تلقائياً (دالة `record_instructor_attendance()` بقاعدة البيانات) — حضور لمدرب بالحصة اليومية بيضيف أجر يومه، غياب لمدرب براتب شهري بيخصم مبلغ الغياب المحدد له.
- **View `instructor_balances`** — نفس نمط `freelancer_balances`، بيحسب الرصيد الحالي لكل مدرب من مجموع حركات سجله المالي.

## صفحة `/admin/reports` — جديدة

تقارير حية (كل الأرقام محسوبة مباشرة من `children`/`enrollments` الموجودة، بدون أي جدول تجميع منفصل مخزّن):

- **توزيع الأعمار** — محسوب من `date_of_birth` (سنة كاملة لكل عمر)، مع فئة "غير محدد" لمن ما عنده تاريخ ميلاد مسجّل.
- **توزيع الجنس** و**توزيع الجنسيات** — بنفس منطق "غير محدد" للقيم الفارغة.
- **عدد المسجلين لكل نشاط** — الاشتراكات النشطة، مع عدد إضافي بين قوسين لأي اشتراكات "قيد المراجعة" لنفس النشاط.
- **ملخص مالي حسب النشاط** — مجموع `price_paid` لكل نشاط، بالإضافة للإجمالي الكلي.

## قواعد العمل (Business Rules)

- المتدرب ما ينقدر ينسجل حضوره مرتين لنفس النشاط بنفس اليوم — المسح الثاني بس بيحدّث وقت التسجيل، ما بينشئ سجل مكرر.
- المتدرب بيقدر يشترك بعدة أنشطة بنفس الوقت — ما في حد أقصى لعدد الاشتراكات.
- كل نشاط ممكن يكون عنده أكتر من باقة حصص/سعر، وأكتر من خانة زمنية أسبوعية.
- **حذف نشاط بيحذف معه كل الاشتراكات المرتبطة فيه، سجل حضورها، وخاناتها الزمنية بالكامل** — المتدرب نفسه ما بينحذف.
- حذف متدرب بيحذف كل اشتراكاته وسجل حضوره بكل الأنشطة (مختلف عن **أرشفة** متدرب، يلي بتحافظ على كل بياناته وسجله وبس بتمنع حضوره لحد ما يترجّع).
- تجديد اشتراك = إضافة على نفس السجل الموجود، مش إنشاء سجل جديد.
- لا يوجد حد أقصى لعدد الأنشطة أو الاشتراكات لكل متدرب.
- **حضور المتدرب ما بينمنع بسبب انتهاء الحصص أو انتهاء المدة — بس بيظهر تحذير.** بالمقابل، **بينمنع بالكامل** (بدون أي استثناء) لو الاشتراك مش `active` (قيد المراجعة/ملغى/مرفوض) أو المتدرب مؤرشف — هاي استثناءات واعية ومقصودة عن قاعدة "الحضور ما بينمنع"، مفصولة بوضوح بالكود عن بعض وعن التحذيرات.

## هيكل قاعدة البيانات (مبسّط، مش SQL)

```
Activities (الأنشطة)
- id, name, emoji, instructor_name, instructor_id (مربوط بجدول instructors), schedule_text

Activity_packages (باقات كل نشاط)
- id, activity_id, session_count, price, sessions_per_week (اختياري)

Activity_time_slots (خانات زمنية أسبوعية لكل نشاط — جديد)
- id, activity_id, day_of_week, start_time, end_time (دايماً ساعة كاملة)
- UNIQUE(activity_id, day_of_week, start_time)

Enrollment_slot_assignments (تعيين اشتراك لخانة/خانات — جديد)
- id, enrollment_id, slot_id — UNIQUE(enrollment_id, slot_id)

Children (المتدربون — الاسم بالكود ضل children عمداً)
- id, full_name, photo_base64, qr_token, parent_phone (إلزامي)
- date_of_birth, nationality, gender (male/female) — اختياري بقاعدة البيانات
- parent_full_name, relationship_to_child, parent_email, address — اختياري بقاعدة البيانات
- has_medical_condition + medical_condition_details — NULL = "محدّش سأل"
- is_on_medication + medication_details — نفس منطق NULL
- has_special_needs + special_needs_details — نفس منطق NULL، إداري بس (جديد)
- consent_terms_accepted + consent_terms_accepted_at
- consent_marketing_photos
- archived_at + archived_reason — أرشفة/استرجاع (جديد)
- قيود CHECK تضمن كل حقل "تفاصيل" موجود إذا وبس إذا الحقل البولياني المرتبط فيه = true (أو الاثنين NULL سوا لحالة الأرشفة)

Enrollments (اشتراك متدرب بنشاط)
- id, child_id, activity_id, package_id
- sessions_total, sessions_used_offset, price_paid
- expiry_date — تاريخ انتهاء بالوقت، NULL = بدون حد زمني
- status (pending_approval / active / cancelled / rejected — جديد) + status_reason + status_changed_at
- sessions_per_week_override — تجاوز يدوي لعدد الحصص الأسبوعية الفعلي (جديد)

Activity_attendance (سجل حضور المتدربين)
- id, enrollment_id, attendance_date, status (حاضر/غايب)

Instructors (المدربون الداخليون)
- id, name, contact, active, deactivation_reason (جديد)
- pay_type (per_session/monthly), default_rate_per_day, monthly_salary, monthly_absence_deduction

Instructor_attendance (حضور/غياب المدربين الداخليين يوماً بيوم)
- id, instructor_id, attendance_date, status

Instructor_ledger_entries (السجل المالي للمدربين الداخليين)
- id, instructor_id, entry_type, amount, balance_after, note, related_attendance_id, reversed_entry_id
- View: instructor_balances

Admin_settings / Staff_settings (كلمتا مرور صفحتي الإدارة والموظفين)
- password (صف واحد بس لكل جدول)
```

(جداول وحدة Freelancers الكاملة — `freelancers` (بالإضافة لـ`deactivation_reason` الجديد)، `level_default_pricing`، `freelancer_pricing_overrides`، `freelancer_sessions`، `freelancer_session_level_counts`، `session_qr_tokens`، `freelancer_ledger_entries`، `freelancer_payment_type_history`، `freelancer_settings`، `freelancer_notifications`، View `freelancer_balances` — تفصيلها الكامل بقسم "وحدة Freelancers" تحت، بدون تغيير عن السابق عدا إضافة `deactivation_reason`.)

ملاحظة: "عدد الحصص المستخدمة" و"الرصيد المتبقي" مش أعمدة بالجدول — دايماً بينحسبوا من عدد صفوف الحضور الفعلية + `sessions_used_offset`. نفس المنطق ينطبق على `date_expired` (بينحسب لحظياً من `expiry_date < CURRENT_DATE`).

**قرار مهم اتخذ بجلسة سابقة**: كل الحقول الجديدة على `children` (عدا `parent_phone`) نزلت **قابلة للفراغ (nullable) بقاعدة البيانات**، رغم إنها **إلزامية بفورم التسجيل الجديد**. السبب: متدربين حقيقيين مسجلين قبل هالتحديثات، وفرض NOT NULL كان رح يجبرنا نحط قيم وهمية (خصوصاً بالحقول الطبية/الاحتياجات الخاصة) — وهاد خطير لأنه `false` بحقل "عنده حالة صحية؟" بيوهم الموظف إنه "تأكدنا إنه ما في حالة صحية"، بينما الحقيقة إنه محدّش سأل أصلاً. نفس القرار انطبق بعدين على `has_special_needs`.

**⚠️ درس مهم اتعلّمناه**: توثيق `schema.sql` مش ضمان إنه القاعدة الحقيقية متطابقة معه فعلياً (صار في أكتر من مرة عمود موثّق بالملف بس ما كان منفّذ فعلياً ضد القاعدة الحقيقية). **الدرس**: أي شك بعمود، لازم يتأكد مباشرة عبر `information_schema.columns`/`pg_constraint` ضد القاعدة الحقيقية، مش بالاعتماد على الملف بس — نفس هالمبدأ انطبّق بدقة على كل ميزة جديدة هالمرحلة (Phase B، الأرشفة/الجدولة، رابط الاحتياجات الخاصة): كل migration انكتب كملف `.sql` منفصل "للمراجعة بس" (Claude ما بيشغّل DDL مباشرة على القاعدة الحية إطلاقاً)، عمرو بيشغّله يدوياً بـNeon SQL Editor، وبعدين Claude بيتأكد مباشرة عبر استعلامات قراءة فقط إنه التنفيذ طابق الملف تماماً قبل ما يبني أي كود فوقه.

## تدفق الاستخدام (Workflow) — النظام الأساسي

```
ولي الأمر يفتح رابط التسجيل (/register أو /register/special)
        ↓
يعبّي فورم الـ4 خطوات ويختار نشاط/أنشطة + باقة لكل وحدة
        ↓
يتولّد كود QR فريد للمتدرب فوراً (قابل للتحميل)، بس الاشتراك بحالة "قيد المراجعة"
        ↓
الإدارة تفتح "التسجيلات الجديدة بانتظار الموافقة" بـ/admin وتوافق (أو ترفض بسبب)
        ↓
الموظف يسجّل دخول بـ/staff، يفتح تفقد الأكاديمية، يختار النشاط الجاري، ويمسح QR
        ↓
النظام يسجّل الحضور (مرفوض بالكامل لو الاشتراك لسا قيد المراجعة أو المتدرب مؤرشف؛ تحذير بس لو خلصت الحصص أو المدة)
        ↓
الإدارة تفتح صف المتدرب وتشوف كم حصة استخدم وكم باقي، بياناته الكاملة، وجدوله الزمني المعيّن
        ↓
إذا خلصت الحصص أو المدة، الإدارة تجدّد الاشتراك — expiry_date بيتحسب من جديد حسب أولوية override → خانات فعلية → باقة
```

## أشياء أساسية ما لازم تتغيّر إلا إذا طلب المستخدم صراحة

- المسح عبر QR هو الوسيلة الأساسية لتسجيل الحضور — بس موجود أيضاً تبديل يدوي حاضر/غايب من قائمة الحضور نفسها، وهاد مقصود ومطلوب يضل موجود.
- **الحضور ما بينمنع عند انتهاء الاشتراك (تحصص أو مدة) — تحذير بس.** هاي القاعدة عندها الآن **استثناءان واعيان ومقصودان فقط**، لازم يضلّوا مفصولين بوضوح بالكود عن بعض وعن التحذيرات: (1) اشتراك مش `active` (قيد المراجعة/ملغى/مرفوض)، و(2) متدرب مؤرشف. أي ميزة مستقبلية تحتاج "حظر" حضور لازم تسأل صراحة قبل ما تضاف كاستثناء ثالث.
- عدد الحصص المستخدمة ما بينخزن كعداد مباشر — دايماً محسوب من سجل الحضور + الحصص اليدوية.
- **رابطا التسجيل (`/register` و`/register/special`) عامين تماماً بدون تسجيل دخول** — الوحيدين بكل التطبيق.
- **`/attendance` محمية بدخول الموظف** (بعد ما كانت عامة بالكامل بجلسة قديمة).
- الصور تبقى داخل قاعدة البيانات نفسها (base64)، ما في خدمة تخزين صور خارجية.
- كلمة مرور واحدة مشتركة لصفحة الإدارة، وكلمة مرور مشتركة منفصلة تماماً للموظفين (مش حسابات مستخدمين فردية بأي من الاثنين).
- **الحقول الطبية/الشخصية/الاحتياجات الخاصة إلزامية بالفورم بس اختيارية بقاعدة البيانات عمداً** — أي تعديل مستقبلي عليها لازم يحترم نفس المنطق (NULL يعني "لسا ما انسأل"، مش "لا").
- **علامة الاحتياجات الخاصة إداري بالكامل** — أي تطوير مستقبلي عليها لازم يحافظ على غيابها التام من صفحة المسح وبطاقة QR.
- **الترجمة (عربي/إنجليزي) موجودة على كل الصفحات** — أي نص جديد ظاهر للمستخدم لازم يُضاف كمفتاح بقاموس JSON المناسب بدل ما يُكتب كنص مباشر بالكود.
- **ساعات الدوام المسموحة للخانات الزمنية سياسة بمستوى الكود (`lib/businessHours.js`)، مش قيد بقاعدة البيانات** — عمداً، عشان تتغيّر بسهولة بدون migration.
- **كل migration بقاعدة البيانات لازم يكون ملف `.sql` منفصل "للمراجعة بس"** — Claude Code ما بيشغّل DDL مباشرة على القاعدة الحية أبداً إلا بإذن صريح لكل حالة.

## المبادئ المعمارية (Architecture Principles)

هاد القسم مو للمستخدمين — هو توجيه لأي AI بيناقش تطوير هالمشروع. أي اقتراح تطوير لازم يحترم:

- **لا تكرار للبيانات (Single Source of Truth)** — كل معلومة إلها مكان واحد بس تتخزن فيه.
- **الحسابات تُستنتج، ما تُخزن** — إذا قيمة ممكن تنحسب من بيانات موجودة أصلاً، ما تقترح عمود/جدول جديد يخزنها كعداد منفصل.
- **الإضافة أفضل من التعديل** — لما تحتاج تغيير بالبنية، فضّل إضافة عمود/جدول جديد على تعديل جدول موجود أو كسر التوافق مع بيانات حالية.
- **الموبايل أولاً** — أي ميزة جديدة لازم تشتغل منيح على شاشة موبايل صغيرة قبل ما تفكر بالحاسوب.
- **تعقيد أقل مقابل فايدة أكبر** — لا تقترح حل معقد لمشكلة صغيرة يمكن حلها بشكل أبسط.
- **تجنّب تضارب المصالح بتصميم الصلاحيات** — الفعل الحساس لازم يكون بإيد طرف مستقل، مش الطرف يلي عنده مصلحة بالنتيجة (مثال حقيقي: مسح QR انتقل من المدرب/الفريلانسر لنفسه إلى الموظف المستقل).

## الأولويات

**Priority A**
- سرعة الاستخدام بالنادي (خصوصاً وقت المسح).
- البساطة — أقل عدد ضغطات ممكن لإنجاز أي مهمة.

**Priority B**
- المظهر الجميل والمناسب للأطفال والرياضة.

**Priority C**
- ميزات ثانوية/تحسينات تجميلية.

## قيود المشروع (Constraints)

- المشروع صغير ومقصود يضل بسيط بالبنية.
- ما بدنا خدمات خارجية مدفوعة إضافية (لا معالج دفع، لا تخزين صور منفصل، لا نظام إشعارات SMS/واتساب حالياً).
- نفضّل الاعتماد على Next.js + قاعدة بيانات وحدة (Postgres/Neon) بس.
- نفضّل حلول تنقدر تنشر على Vercel بسهولة بدون بنية تحتية إضافية.

## خارطة الطريق (Roadmap)

**Phase 1 — ✓ منجز (النظام الأساسي)**
- التسجيل + اختيار الأنشطة، توليد QR ومشاركته كصورة، تسجيل الحضور بالمسح + يدوي، تتبّع الحصص والاشتراكات، حماية صفحة الإدارة بكلمة مرور.

**Phase 1.5 — ✓ منجز (وحدة Freelancers كاملة + وحدة الموظفين)**
- Backend وواجهات كاملتين لوحدة Freelancers (حجز، موافقة/رفض، مسح، إغلاق، سجل مالي، إشعارات).
- نظام دخول موظفين منفصل (`/staff`) يحمي `/attendance` ومسح الفريلانسرز سوا — أغلق ثغرة تضارب مصالح حقيقية (المدرب كان يمسح لنفسه).

**Phase 1.6 — ✓ منجز (فورم تسجيل موسّع + تسمية "متدرب" + انتهاء بالوقت + ترجمة كاملة)**
- حقول شخصية/طبية/موافقات جديدة على `children`، `sessions_per_week` على الباقات، `expiry_date` على الاشتراكات.
- `/register` فورم متعدد الخطوات (4 خطوات)، تسمية "طفل" → "متدرب" بكل مكان.
- ترجمة كاملة عربي/إنجليزي (`bwa_locale` cookie، React Context، قواميس JSON) على كل الصفحات.

**Phase 1.7 — ✓ منجز (موافقة الإدارة + أرشفة + جدولة + احتياجات خاصة + تقارير + سجل مدربين موسّع)**
- workflow موافقة إدارة كامل على كل تسجيل عام جديد (حالات `enrollments` الأربعة، حظر صريح للحضور غير النشط، قسم موافقة مجمّع حسب المتدرب).
- علامة الاحتياجات الخاصة (إداري بالكامل) + رابط تسجيل منفصل `/register/special` (وصول عام، مربوط بزر بالصفحة الرئيسية + زر نسخ رابط منفصل، عنوان/معاينة مطابقين تماماً للفورم العادي بدون كشف المعلومة).
- أرشفة/استرجاع متدرب (حظر حضور كامل، قائمة `/admin` مقسومة نشطين/مؤرشفين قابلة للطي).
- نظام جدولة كامل (خانات زمنية أسبوعية لكل نشاط، تعيين اشتراكات لخانات، أولوية حساب عدد الحصص الأسبوعية الفعلي، صفحة `/admin/schedule`).
- سجل مالي بسبب حر + سبب تعطيل إلزامي لكلا نوعي المدربين (داخليين وفريلانسرز)، واجهة كاملة مكشوفة بـ`/admin/instructors`.
- صفحة `/admin/reports` (أعمار، جنس، جنسيات، مسجلين لكل نشاط، ملخص مالي).
- إصلاح فلترة `archived_at` الناقصة بـ`/api/child-list`، وشريط ترحيبي متحرك (CSS بحت) أعلى `/register`.

**Phase 2 — أفكار قيد النقاش (غير منفّذة)**
- **إشعارات تلقائية (واتساب/SMS) لتذكير الأهل بانتهاء الاشتراك** — **نوقشت، غير منفّذة**. صار عندنا الآن `expiry_date` مخزّن فعلياً، فهاي الميزة صارت أسهل تقنياً من قبل، بس لسا مجرد فكرة مطروحة.
- إعادة تصميم شكل التطبيق (المظهر الحالي "مربعات مبتدئين" — نقاش مفتوح لسا ما بلّش، عدا شريط `/register` الترحيبي يلي أخذ هوية بصرية "Clean Sporty" منفصلة كبداية محدودة النطاق).
- نظام حضور/غياب للمدربين الداخليين أنفسهم عبر بصمة/QR ذاتي (مختلف عن التسجيل اليدوي الحالي من الموظف بـ`/staff/instructor-attendance`) — **مؤجّل عمداً**، لأنه فيه نفس فخ تضارب المصالح يلي انسدّ بالفريلانسرز.

**Phase 3 — أفكار بعيدة المدى**
- بوابة يشوف فيها الأهل حصص متدربهم بأنفسهم.
- صلاحيات موظفين متعددة (حسابات فردية بدل كلمة مرور مشتركة).
- تطبيق موبايل مخصص.

## فلسفة التصميم

واجهة المستخدم لازم تضل:
- مناسبة للاستخدام من الموبايل بالدرجة الأولى.
- أزرار كبيرة وواضحة.
- أقل عدد شاشات/خطوات ممكنة لإنجاز أي مهمة.
- مفهومة لموظف غير تقني بدون أي تدريب.

**ملاحظة مفتوحة (نقاش لسا ما بلّش بشكل شامل)**: المظهر البصري العام وُصف سابقاً بـ"مربعات مبتدئين" — في نية لإعادة تصميم شكل التطبيق (ألوان، تايبوغرافي، تفاصيل بصرية) بجلسة نقاش منفصلة قبل أي تنفيذ واسع. شريط `/register` الترحيبي المتحرك مثال محدود النطاق على هوية بصرية جديدة ("Clean Sporty" كحلي/أزرق كهربائي)، بس ما اتعمم على باقي الصفحات لسا.

## أشياء موجودة بالكود بس ممكن تتناقشوا فيها كتطوير مستقبلي

- **ما في نظام دفع إلكتروني حقيقي** — تسجيل "المبلغ المدفوع" يدوي بس (رقم يكتبه الأدمن)، ما في ربط ببوابة دفع حقيقية.
- **ما في صفحة للأهل يتابعوا فيها حصص متدربهم بأنفسهم** — هلق هاد الشي بس ظاهر للإدارة والموظفين.
- **كلمتي مرور مشتركتين بس (أدمن + موظف)** — ما في حسابات مستخدمين فردية أو صلاحيات متفاوتة داخل كل فئة. المدربين المستقلين (Freelancers) استثناء — عندهم حسابات فردية فعلية (رقم جوال + PIN).
- **ما في تنبيهات تلقائية خارجية** (واتساب/SMS/إيميل) — الإشعارات الموجودة (لطلبات الفريلانسرز) داخل التطبيق بس.
- **زر "حذف كل البيانات" (النظام الأساسي) قوي جداً وبدون نسخة احتياطية** — أي حذف نهائي وما ممكن يترجع. **ملاحظة**: الأرشفة الجديدة بديل أخف وقابل للتراجع لحالات كتيرة كانت سابقاً بتحتاج حذف كامل.
- **الإغلاق التلقائي لجلسات الفريلانسرز (cron) لسا ما انتجرب فعلياً بجلسة منتهية بوقت حقيقي** — الآلية مبنية ومنشورة (GitHub Actions كل 10 دقائق)، بس ما في تأكيد ميداني حتى الآن.
- **ما في وسيلة حذف متدرب فردي بالكامل من واجهة `/admin`** — الخيارات المتاحة إما إلغاء اشتراك/أرشفة (قابلين للتراجع)، أو "حذف كل البيانات" (يمسح كل شي دفعة وحدة). أي بيانات تجريبية (`__QA_TEST_*`) اتخلقت أثناء الفحص الحي بتضل موجودة بالقاعدة الحقيقية لنفس السبب.
- **بعض النصوص لسا ممكن يكون فاتها الترجمة** — التغطية شملت كل الصفحات الأساسية، بس بحجم كود بهالضخامة، احتمال وجود نص متبقّي وارد.

## وحدة الموظفين (Staff)

### 1. السبب

كان في ثغرة أمنية حقيقية: المدرب المستقل (Freelancer) يقدر يمسح رموز QR جلساته بنفسه، وهو نفسه يلي بيتحدد عليه المبلغ يلي بيدفعه حسب نتيجة المسح. الحل: نقل صلاحية المسح لموظف مستقل (النادي)، مو المدرب نفسه. بالتوازي، اتوحّد دخول الموظفين تحت بوابة واحدة تحمي `/attendance` ومسح الفريلانسرز سوا.

### 2. البنية

- جدول `staff_settings` (كلمة مرور مشتركة واحدة، نفس نمط `admin_settings`).
- كوكي جلسة `bwa_staff_session` (نفس نمط `bwa_admin_session` — مقارنة نصية مباشرة، مش hash، لأسباب Edge Runtime).
- `middleware.js`: حماية `/staff/*` بالكامل (عدا `/staff/login`)، وحماية `/attendance` بالكامل (الصفحة + `POST /api/attendance`، بينما `GET /api/attendance` ضل عام لأنه `/history` بتعتمد عليه).
- كلمة مرور الموظفين تدار من `/admin` فقط (زر "🔑 كلمة مرور الموظف").

### 3. الصفحات

| المسار | الوصف |
|---|---|
| `/staff/login` | فورم دخول بكلمة مرور واحدة |
| `/staff` | بوابة بعد الدخول: زرين ("تفقد الأكاديمية" → `/attendance`، "تفقد الفريلانسرز" → `/staff/freelancer-scan`) + خروج |
| `/staff/freelancer-scan` | قائمة جلسات الفريلانسرز الجاهزة للمسح |
| `/staff/freelancer-scan/[id]` | صفحة مسح فعلية لجلسة معيّنة |
| `/staff/instructor-attendance` | تسجيل حضور/غياب المدربين الداخليين يوماً بيوم (وصول مباشر بالرابط، مش خيار ظاهر ببوابة `/staff`) |

### 4. الأثر على وحدة Freelancers

- `POST /api/freelancer/sessions/[id]/scan` معطّل نهائياً (403 دايماً) — المدرب ما عاد يقدر يمسح لنفسه.
- صفحة `/freelancer/sessions/[id]/scan` محذوفة بالكامل من الكود.
- endpoint `POST /api/admin/freelancer-sessions/[id]/scan` — نفس منطق `scan_freelancer_qr_token()`، بس محمي بكوكي الموظف عبر middleware.

## وحدة Freelancers (المدربون المستقلون)

### 1. نظرة عامة

الفريلانسرز مدربون مستقلون (مش موظفين بالأكاديمية) بيستأجروا وقت مسبح لحصصهم الخاصة. الدورة الكاملة: **حجز موعد ← موافقة/رفض من الإدارة ← تسجيل حضور الأطفال بمسح QR من جهة الموظف ← إغلاق الجلسة (يدوي أو تلقائي) ← محاسبة تلقائية بالـledger ← عرض/دفعات/عكس حركات من جهة الإدارة**.

### 2. الجداول

`freelancers` (بالإضافة لـ`deactivation_reason`)، `level_default_pricing`، `freelancer_pricing_overrides`، `freelancer_sessions`، `freelancer_session_level_counts`، `session_qr_tokens`، `freelancer_ledger_entries`، `freelancer_payment_type_history`، `freelancer_settings`، `freelancer_notifications`، View: `freelancer_balances`.

### 3. دوال قاعدة البيانات (SQL Functions)

`reset_freelancer_test_data()`، `close_freelancer_session()`، `create_freelancer_session_request()`، `scan_freelancer_qr_token()`، `add_freelancer_ledger_entry()`.

### 4. حالة الواجهات (UI)

**واجهة المدرب** (`/freelancer/*`, خلف دخول رقم جوال + PIN): `/freelancer/login`، `/freelancer` (لوحة رئيسية)، `/freelancer/book`، `/freelancer/sessions`، `/freelancer/sessions/[id]`، `/freelancer/ledger`، `/freelancer/notifications`.

**واجهة الموظف** (`/staff/*`): `/staff/freelancer-scan`، `/staff/freelancer-scan/[id]`.

**واجهة الإدارة** (`/admin`, `/admin/freelancers`): قسم "طلبات الفريلانسرز" بالصفحة الرئيسية، `/admin/freelancers` (قائمة المدربين، تفعيل/تعطيل بسبب، نوع الدفع، تصفير PIN، أسعار خاصة، السجل المالي الكامل، الأسعار الافتراضية للمستويات).

## الترجمة (عربي/إنجليزي)

### 1. الفكرة

كل صفحة بالتطبيق صار فيها زر تبديل لغة يدوي، القيمة بتتحفظ بكوكي وبتضل ثابتة وانت بتتنقل بين الصفحات. الإنجليزي **انعكاس حقيقي لاتجاه الصفحة (LTR)**، مش عربي بمحتوى إنجليزي.

### 2. البنية التقنية

- **كوكي واحد** `bwa_locale`، بيتقرا **من السيرفر** (`app/layout.js`) عشان يحدد `<html dir="rtl|ltr" lang="ar|en">` قبل حتى ما توصل الصفحة للمتصفح.
- **`lib/locale/LocaleContext.js`**: `LocaleProvider` (React Context)؛ `useLocale()`؛ `useTranslations(section)` بيرجع دالة `t(key, vars)`.
- **صفحات فيها محتوى ثابت مهم لمحركات البحث/معاينة الروابط** (`/register`, `/register/special`) بتصدّر `generateMetadata()` بمستوى السيرفر (تقرا نفس كوكي `bwa_locale` مباشرة) بدل الاعتماد بس على تحديث `document.title` بجافاسكربت من جهة العميل — **درس مهم اتعلّمناه**: تحديث `document.title` بـ`useEffect` بعد التحميل بيخسر أحياناً "سباق" مع آلية Next.js الداخلية يلي بترجّع عنوان الـlayout الأب عند أي تنقّل (حتى client-side)، فالعنوان الصحيح ممكن يظهر متأخر بثانية أو يبين غلط للحظة. الحل الموثوق: `generateMetadata()` بمستوى الصفحة نفسها (سيرفر)، بدل الاعتماد الكامل على تعديل جافاسكربت من جهة العميل. نفس المبدأ انطبق على `openGraph.title`/`openGraph.description` (معاينة مشاركة الرابط) — لازم تتحدد صراحة بكل صفحة، وإلا بترث القيم الافتراضية من الـlayout الجذري تلقائياً.
- **القواميس**: `lib/locale/dictionaries/{common,register,staff,admin,freelancer}.json` — كل ملف فيه `{ "ar": {...}, "en": {...} }`، مقسّمة حسب الجمهور. `/register/special` بيستخدم قاموس `register` نفسه (مافي قاموس منفصل له).
- **لا مكتبة i18n خارجية** — الحل كله JSON عادي + React Context.
- **محتوى قاعدة البيانات ما بينترجم أبداً** (اسم المتدرب، النشاط...) — الاستثناء الوحيد المتعمّد: نصوص "المواعيد" (`schedule_text`) للأنشطة السبعة المزروعة تلقائياً بس، عبر جدول ترجمة محدود ثابت بالكود (`lib/locale/knownScheduleText.js`)، مش ترجمة عامة لأي محتوى يكتبه الأدمن.
- **تنسيق التاريخ/الوقت** بيتغيّر حسب اللغة (`ar-EG` مقابل `en-US`).

### 3. قاعدة مهمة لأي تطوير مستقبلي

**أي نص جديد ظاهر للمستخدم لازم يُضاف كمفتاح جديد بقاموس JSON المناسب (عربي+إنجليزي سوا)، وينستخدم عبر `t('theKey')`.** لو النص مشترك بين أكتر من قسم، ينضاف لـ`common.json`.

### 4. CSS واتجاه الصفحة

أي CSS جديد لازم يستخدم خصائص محايدة الاتجاه (`margin-inline-start/end`, `inset-inline-start/end`, `text-align: start/end`) بدل `left`/`right` المباشرة — هاد النمط مستخدم بكل الكود الموجود، وانطبق كمان على حركة الفقاعات بشريط `/register` الترحيبي (بدون أي قاعدة CSS إضافية لعكس الاتجاه).

---

## شو بدي من Claude أثناء النقاش

لا أريد كتابة كود بهالمحادثة. أريد منك:

- نقد الفكرة يلي رح أطرحها بصراحة.
- اقتراح مشاكل مستقبلية ممكن تصير.
- مقارنة بدائل مختلفة للحل.
- التفكير بتجربة المستخدم (خصوصاً للموظف غير التقني وبالنادي المزعج/الصاخب).
- اقتراح أفضل workflow لأي ميزة جديدة.
- التنبيه إذا كان اقتراح معيّن رح يأثر على الأداء أو قاعدة البيانات.
- **ما تقترح تعديلات تغيّر فلسفة النظام** (القسم أعلاه "أشياء أساسية ما لازم تتغيّر") **إلا إذا سألتك عنها صراحة**.

لما نوصل لقرار واضح، بارجع فيه لمحادثة Claude Code للتنفيذ الفعلي بالكود.
