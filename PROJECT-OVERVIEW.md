# نظرة شاملة على مشروع Black Whale Academy

هاد ملف تشرح فيه طريقة عمل التطبيق بالكامل — مصمّم عشان تفتحه بمحادثة Claude Chat عادية (مو Claude Code) وتناقش فيه أفكار تطوير جديدة، بدون ما تحتاج وصول لكود المشروع. لما توصلوا لقرار، ارجع فيه لهون (Claude Code) للتنفيذ الفعلي.

---

## شو هو التطبيق

تطبيق ويب (Next.js) لإدارة أكاديمية رياضية للأطفال — بيغطي تسجيل الأطفال، الاشتراك بأنشطة رياضية متعددة (سباحة، كرة قدم، كاراتيه...)، تتبّع الحصص المدفوعة والمستخدمة، وتسجيل الحضور اليومي عبر مسح كود QR. بالإضافة لوحدة كاملة منفصلة لإدارة المدربين المستقلين (Freelancers) يلي بيستأجروا وقت مسبح. البيانات محفوظة بقاعدة بيانات Postgres (مستضافة على Neon)، والتطبيق منشور على Vercel.

## الفئات اللي بتستخدم التطبيق

كل فئة عندها رابط مختلف، وكل رابط مصمم يعطيها بالضبط الصلاحيات يلي بتحتاجها ومش أكتر:

### 1. الأهل — رابط `/register` (عام، بدون كلمة مرور)
- فورم تسجيل: اسم الطفل، رقم تواصل، صورة.
- يختاروا نشاط واحد أو أكتر (مو بس نشاط واحد)، ولكل نشاط يختاروا "باقة" (مثلاً 8 حصص/300 درهم أو 12 حصة/400 درهم).
- بعد التسجيل، بيتولّد كود QR فريد للطفل، وبيقدروا يحمّلوه كصورة (PNG) لمشاركته أو حفظه.
- هالصفحة مقصود إنها معزولة تماماً — ما فيها أي رابط رجوع لباقي التطبيق، والأهل ما بشوفوا أي معلومة إدارية.
- **هاي الصفحة الوحيدة يلي ضلّت عامة بالكامل بدون تغيير** طول المشروع.

### 2. موظفي النادي — رابط `/staff` (محمي بكلمة مرور مشتركة خاصة بالموظفين، منفصلة عن كلمة مرور الإدارة)
- **جديد بالكامل** — قبل هيك كان `/attendance` عام بدون أي دخول، بس صار محمي عمداً (تفصيل السبب تحت بقسم "وحدة الموظفين").
- بعد الدخول، بوابة (`/staff`) فيها خيارين:
  - **📋 تفقد الأكاديمية** → يوديك لـ`/attendance` (نفس التدفق الأصلي بالكامل، بدون أي تغيير بالمنطق نفسه، بس صار محمي بدخول).
  - **🏊 تفقد الفريلانسرز** → يوديك لـ`/staff/freelancer-scan` (قائمة جلسات الفريلانسرز الجاهزة للمسح، وصفحة مسح مخصصة لكل جلسة).
- كلمة مرور الموظفين تتغيّر من `/admin` (زر "🔑 كلمة مرور الموظف")، مش من داخل `/staff` نفسها.

### 3. المدربين (النظام الأساسي) — عبر `/attendance` (محمي، خلف دخول الموظف)
- المدرب/الموظف يختار "شو النشاط الشغال هلق" (لازم يختار، مو اختياري) — هاد بيحدد لأي نشاط رح ينسجل الحضور.
- بيفتح كاميرا الموبايل ويمسح أكواد QR الأطفال.
- كل مسح ناجح بيسمع صوت مختلف حسب الحالة:
  - **نجاح عادي**: صوت بيب حاد مزدوج.
  - **الطفل خلصت حصصه** (بس التسجيل ينحسب برضه، مش منع): 3 بيبات متوسطة متتالية.
  - **كود غير معروف أو الطفل مش مسجل بهالنشاط تحديداً**: طنّة واحدة طويلة منخفضة.
- في جدول تحت الكاميرا يعرض كل الأطفال المسجلين بهالنشاط، مع إمكانية تبديل حاضر/غايب **يدوياً** كمان (بدون مسح QR إطلاقاً — زر بالجدول نفسه).
- ما فيه أي وصول لصفحة الإدارة أو الأسعار أو بيانات الأطفال الكاملة.

### 4. الإدارة — رابط `/admin` (محمي بكلمة مرور مشتركة خاصة بالإدارة)
- **كلمة المرور** محفوظة بقاعدة البيانات نفسها (مش env variable على Vercel)، وبتقدر تغيّرها من جوا الصفحة نفسها (زر "🔑 تغيير كلمة المرور") بدون أي تدخل تقني. **لسا كلمة المرور الافتراضية `admin123` — لازم تتغيّر (بند معلّق).**
- **إدارة الأنشطة**: إضافة/تعديل/حذف نشاط (اسم، إيموجي، اسم المدرب، المواعيد)، وإضافة أكتر من "باقة" حصص/سعر لنفس النشاط.
- **زر تعبئة تلقائية**: بيضيف 7 أنشطة جاهزة (سباحة، Baby Swimming، كرة قدم، كرة سلة، جمباز، كاراتيه، كيك بوكسينج) بمواعيدها وأسعارها الحقيقية بضغطة وحدة.
- **قائمة الأطفال**: كل طفل بصير قدك توسّع صفه لتشوف كل اشتراكاته (نشاط + كم حصة استخدم/باقي + المبلغ المدفوع)، وتقدر:
  - تجدّد اشتراك (إضافة حصص جديدة فوق الموجودة).
  - تلغي اشتراك بنشاط معيّن.
  - تضيف اشتراك بنشاط جديد.
  - **تسجّل حصص "تفقد يدوي"**: لو الطفل كان يتحضر ورقياً قبل ما تبلّشوا تستخدموا النظام، تقدر تحط عدد الحصص يلي حضرها يدوياً، وبتنضاف تلقائياً فوق أي حصص جديدة عبر مسح QR.
- **طباعة/تحميل بطاقة QR** لكل طفل (صورة PNG كاملة: صورة الطفل + الاسم + الكود).
- **إدارة المدربين المستقلين (Freelancers)** — زر "👥 إدارة المدربين" يوديك لـ`/admin/freelancers` (تفصيل كامل بقسم Freelancers تحت).
- **طلبات الفريلانسرز**: قسم مباشر بالصفحة الرئيسية للإدارة، بيعرض كل الجلسات المعلّقة، تفاصيل الأعداد المطلوبة لكل مستوى، والتكلفة المتوقعة الإجمالية (محسوبة حسب سعر كل مدرب الفعلي — افتراضي أو خاص)، مع زري موافقة/رفض.
- **زر "حذف كل البيانات"**: يمسح كل شي (أطفال، أنشطة، اشتراكات، حضور) نهائياً — محمي بكتابة نص تأكيد يدوي.
- **زر "تصفير بيانات Freelancers التجريبية"**: يمسح فقط بيانات وحدة Freelancers (المدربين، الجلسات، الـledger، الإشعارات) بدون ما يأثر على النظام الأساسي، محمي بكلمة مرور تصفير منفصلة.

### 5. السجل — رابط `/history` (عام، بدون كلمة مرور)
- استعراض سجل أي يوم سابق، بشكل عام (كل الأنشطة) أو حسب نشاط معيّن.
- `GET /api/attendance` يضل عام تماماً (غير محمي) لأنه هاي الصفحة بتعتمد عليه.

## آلية تتبّع الحصص (أهم قاعدة بالنظام)

- كل اشتراك (طفل + نشاط) عنده "رصيد حصص" = عدد الحصص الكلي (من الباقة المشتراة) ناقص عدد الحصص المستخدمة.
- **عدد الحصص المستخدمة ما بينخزن كرقم منفصل أبداً** — دايماً بينحسب لحظياً من عدد أيام الحضور الفعلية المسجّلة + أي حصص يدوية أضافها الأدمن. هيك ما ممكن يصير خطأ/تضارب بالعداد أبداً.
- تجديد الاشتراك بيضيف على نفس السجل (مش بينشئ اشتراك جديد مكرر).
- إذا الطفل خلصت حصصه ومسح QR، النظام بيسجله حاضر برضه (مش رافض الدخول)، بس بيطلع تحذير واضح للمدرب إنه لازم يجدد الاشتراك.
- الحضور بينسجل لكل نشاط لحاله — يعني نفس الطفل ممكن يكون حاضر بالسباحة الساعة 4 وغايب بالكاراتيه الساعة 6 بنفس اليوم، كسجلين منفصلين تماماً.

## قواعد العمل (Business Rules)

- الطفل ما ينقدر ينسجل حضوره مرتين لنفس النشاط بنفس اليوم — المسح الثاني بس بيحدّث وقت التسجيل، ما بينشئ سجل مكرر.
- الطفل بيقدر يشترك بعدة أنشطة بنفس الوقت — ما في حد أقصى لعدد الاشتراكات.
- كل نشاط ممكن يكون عنده أكتر من باقة حصص/سعر (مثلاً 8 حصص أو 12 حصة لنفس النشاط).
- **حذف نشاط بيحذف معه كل الاشتراكات المرتبطة فيه وسجل حضورها بالكامل** (مش بس منع اشتراكات جديدة) — الطفل نفسه ما بينحذف، بس بيفقد اشتراكه بهاد النشاط تحديداً وتاريخ حضوره فيه.
- حذف طفل بيحذف كل اشتراكاته وسجل حضوره بكل الأنشطة.
- تجديد اشتراك = إضافة على نفس السجل الموجود، مش إنشاء سجل جديد.
- لا يوجد حد أقصى لعدد الأنشطة أو الاشتراكات لكل طفل.
- حضور الطفل ما بينمنع أبداً بسبب انتهاء الحصص — بس بيظهر تحذير.

## هيكل قاعدة البيانات (مبسّط، مش SQL) — النظام الأساسي

```
Activities (الأنشطة)
- id, name, emoji, instructor_name, schedule_text

Activity_packages (باقات كل نشاط)
- id, activity_id, session_count, price

Children (الأطفال)
- id, full_name, parent_contact, photo_base64, qr_token

Enrollments (اشتراك طفل بنشاط)
- id, child_id, activity_id, package_id
- sessions_total (عدد الحصص الكلي المشترى)
- sessions_used_offset (حصص يدوية/ورقية أضافها الأدمن)
- price_paid

Activity_attendance (سجل حضور)
- id, enrollment_id, attendance_date, status (حاضر/غايب)

Admin_settings (إعدادات الإدارة)
- password (كلمة مرور صفحة الإدارة، صف واحد بس)

Staff_settings (إعدادات الموظفين — جديد)
- password (كلمة مرور دخول الموظفين، صف واحد بس، نفس نمط admin_settings بالضبط)
```

ملاحظة: "عدد الحصص المستخدمة" و"الرصيد المتبقي" مش أعمدة بالجدول — دايماً بينحسبوا من عدد صفوف الحضور الفعلية + sessions_used_offset.

**⚠️ درس مهم اتعلّمناه هالجلسة**: عمود `sessions_used_offset` كان موثّق بـ`schema.sql` (عبر `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`) من زمان، بس **ما كان منفّذ فعلياً ضد قاعدة البيانات الحقيقية** — اكتشفنا هيك لما صفحة الإدارة طلعت بتعرض "ما في اشتراكات" لطفلين مسجلين فعلياً (كانت الـAPI بترجع 500 بصمت، والواجهة كانت بتبلع الخطأ). **الدرس**: توثيق schema.sql مش ضمان إنه القاعدة الحقيقية متطابقة معه فعلياً — أي شك بعمود، لازم يتأكد مباشرة عبر `information_schema.columns` ضد القاعدة الحقيقية، مش بالاعتماد على الملف بس.

## تدفق الاستخدام (Workflow) — النظام الأساسي

```
ولي الأمر يفتح رابط التسجيل
        ↓
يعبّي بيانات الطفل ويختار نشاط/أنشطة + باقة لكل وحدة
        ↓
يتولّد كود QR فريد للطفل (قابل للتحميل كصورة)
        ↓
الموظف يسجّل دخول بـ/staff، يفتح تفقد الأكاديمية، يختار النشاط الجاري، ويمسح QR
        ↓
النظام يسجّل الحضور لهاد النشاط تحديداً بهاليوم
        ↓
الإدارة تفتح صف الطفل وتشوف كم حصة استخدم وكم باقي
        ↓
إذا خلصت الحصص، الإدارة تجدّد الاشتراك (تضيف باقة جديدة)
```

## أشياء أساسية ما لازم تتغيّر إلا إذا طلب المستخدم صراحة

- المسح عبر QR هو الوسيلة الأساسية لتسجيل الحضور — بس موجود أيضاً تبديل يدوي حاضر/غايب من قائمة الحضور نفسها (بدون مسح)، وهاد مقصود ومطلوب يضل موجود.
- الحضور ما بينمنع عند انتهاء الاشتراك (تحذير بس، مش حظر).
- عدد الحصص المستخدمة ما بينخزن كعداد مباشر — دايماً محسوب من سجل الحضور + الحصص اليدوية.
- **رابط التسجيل (`/register`) عام تماماً بدون تسجيل دخول** — هاد ضل زي ما هو.
- **`/attendance` صارت محمية بدخول الموظف** (تغيير فلسفي متعمّد صار هالجلسة، بعد ما كانت عامة بالكامل — راجع قسم "وحدة الموظفين" للسبب).
- الصور تبقى داخل قاعدة البيانات نفسها (base64)، ما في خدمة تخزين صور خارجية.
- كلمة مرور واحدة مشتركة لصفحة الإدارة، وكلمة مرور مشتركة منفصلة تماماً للموظفين (مش حسابات مستخدمين فردية بأي من الاثنين).

## المبادئ المعمارية (Architecture Principles)

هاد القسم مو للمستخدمين — هو توجيه لأي AI بيناقش تطوير هالمشروع. أي اقتراح تطوير لازم يحترم:

- **لا تكرار للبيانات (Single Source of Truth)** — كل معلومة إلها مكان واحد بس تتخزن فيه.
- **الحسابات تُستنتج، ما تُخزن** — إذا قيمة ممكن تنحسب من بيانات موجودة أصلاً (متل عدد الحصص المستخدمة)، ما تقترح عمود/جدول جديد يخزنها كعداد منفصل.
- **الإضافة أفضل من التعديل** — لما تحتاج تغيير بالبنية، فضّل إضافة عمود/جدول جديد (زي `sessions_used_offset`، `admin_settings`، `staff_settings`) على تعديل جدول موجود أو كسر التوافق مع بيانات حالية.
- **الموبايل أولاً** — أي ميزة جديدة لازم تشتغل منيح على شاشة موبايل صغيرة قبل ما تفكر بالحاسوب.
- **تعقيد أقل مقابل فايدة أكبر** — لا تقترح حل معقد (خدمة خارجية، جدول جديد، طبقة إضافية) لمشكلة صغيرة يمكن حلها بشكل أبسط.
- **تجنّب تضارب المصالح بتصميم الصلاحيات** — لو شخص/دور معيّن بيتحدد عليه نتيجة مالية أو محاسبية بناءً على فعل يقدر يسويه بنفسه (مثال حقيقي صار هالمشروع: المدرب كان يقدر يمسح رموز QR جلساته بنفسه، وهو نفسه يلي بيتحدد عليه المبلغ يلي بيدفعه حسب نتيجة المسح)، الفعل الحساس لازم يكون بإيد طرف مستقل (موظف/إدارة)، مش الطرف يلي عنده مصلحة بالنتيجة.

## الأولويات

**Priority A**
- سرعة الاستخدام بالنادي (خصوصاً وقت المسح).
- البساطة — أقل عدد ضغطات ممكن لإنجاز أي مهمة.

**Priority B**
- المظهر الجميل والمناسب للأطفال والرياضة.

**Priority C**
- ميزات ثانوية/تحسينات تجميلية.

## قيود المشروع (Constraints)

- المشروع صغير ومقصود يضل بسيط بالبنية.
- ما بدنا خدمات خارجية مدفوعة إضافية (لا معالج دفع، لا تخزين صور منفصل، لا نظام إشعارات SMS/واتساب حالياً).
- نفضّل الاعتماد على Next.js + قاعدة بيانات وحدة (Postgres/Neon) بس.
- نفضّل حلول تنقدر تنشر على Vercel بسهولة بدون بنية تحتية إضافية.

## خارطة الطريق (Roadmap)

**Phase 1 — ✓ منجز (النظام الأساسي)**
- التسجيل + اختيار الأنشطة
- توليد QR ومشاركته كصورة
- تسجيل الحضور بالمسح + يدوي
- تتبّع الحصص والاشتراكات
- حماية صفحة الإدارة بكلمة مرور

**Phase 1.5 — ✓ منجز (وحدة Freelancers كاملة)**
- Backend كامل (الجداول، الدوال، كل الـAPI endpoints)
- واجهة المدرب الكاملة: تسجيل دخول، حجز، قائمة جلسات، تفاصيل جلسة (توليد/تحميل QR، حالة الرموز، إغلاق يدوي)، كشف حساب، إشعارات
- واجهة الإدارة الكاملة: إدارة المدربين (إضافة، تفعيل/تعطيل، نوع الدفع، أسعار خاصة، تصفير PIN)، السجل المالي (دفعات، عكس حركات)، الأسعار الافتراضية، طلبات الموافقة/الرفض بتفاصيل الأعداد والتكلفة المتوقعة
- نظام دخول موظفين منفصل (`/staff`) يحمي `/attendance` ومسح الفريلانسرز سوا
- إغلاق ثغرة أمنية حقيقية: المسح صار حصراً عبر الموظف (مو المدرب لنفسه) — تضارب مصالح محتمل انسدّ

**Phase 2 — أفكار قيد النقاش**
- Dashboard/تقارير وإحصائيات (أكتر نشاط مطلوب، معدل الحضور، الإيرادات)
- إشعارات تلقائية (واتساب/SMS) لتذكير الأهل بانتهاء الاشتراك
- إعادة تصميم شكل التطبيق (المظهر الحالي "مربعات مبتدئين" — نقاش مفتوح لسا ما بلّش)
- نظام حضور/غياب للمدربين أنفسهم (بصمة/QR) — **مؤجّل عمداً**، لأنه فيه نفس فخ تضارب المصالح يلي انسدّ بالفريلانسرز (لو راتب المدرب مرتبط بحضوره، ما لازم يبصم لنفسه بدون طرف مستقل يتحقق)

**Phase 3 — أفكار بعيدة المدى**
- بوابة يشوف فيها الأهل حصص طفلهم بأنفسهم
- صلاحيات موظفين متعددة (حسابات فردية بدل كلمة مرور مشتركة — ينطبق على النظام الأساسي والموظفين والإدارة سوا)
- تطبيق موبايل مخصص

## فلسفة التصميم

واجهة المستخدم لازم تضل:
- مناسبة للاستخدام من الموبايل بالدرجة الأولى.
- أزرار كبيرة وواضحة.
- أقل عدد شاشات/خطوات ممكنة لإنجاز أي مهمة.
- مفهومة لموظف غير تقني بدون أي تدريب.

**ملاحظة مفتوحة (نقاش لسا ما بلّش)**: المظهر البصري الحالي وُصف بـ"مربعات مبتدئين" — في نية لإعادة تصميم شكل التطبيق (ألوان، تايبوغرافي، تفاصيل بصرية) بجلسة نقاش منفصلة قبل أي تنفيذ.

## أشياء موجودة بالكود بس ممكن تتناقشوا فيها كتطوير مستقبلي

- **ما في نظام دفع إلكتروني حقيقي** — تسجيل "المبلغ المدفوع" (النظام الأساسي) و"الدفعات" (Freelancers) يدوي بس (رقم يكتبه الأدمن)، ما في ربط ببوابة دفع حقيقية.
- **ما في صفحة للأهل يتابعوا فيها حصص طفلهم بأنفسهم** — هلق هاد الشي بس ظاهر للإدارة والموظفين.
- **كلمتي مرور مشتركتين بس (أدمن + موظف)** — ما في حسابات مستخدمين فردية أو صلاحيات متفاوتة داخل كل فئة. المدربين المستقلين (Freelancers) استثناء — عندهم حسابات فردية فعلية (رقم جوال + PIN).
- **ما في تنبيهات تلقائية خارجية** (واتساب/SMS/إيميل) — الإشعارات الموجودة (لطلبات الفريلانسرز) داخل التطبيق بس.
- **ما في تقارير/إحصائيات للنظام الأساسي** — البيانات موجودة بالقاعدة بس ما في شاشة تلخّصها. (وحدة Freelancers عندها كشف حساب/سجل مالي، بس بردو بدون رسوم بيانية أو تحليل).
- **زر "حذف كل البيانات" (النظام الأساسي) قوي جداً وبدون نسخة احتياطية** — أي حذف نهائي وما ممكن يترجع. (زر "تصفير Freelancers" مشابه بس محصور بوحدة Freelancers فقط).
- **كلمة مرور الإدارة الافتراضية (`admin123`) لسا ما تغيّرت** — بند معلّق من زمان.
- **الإغلاق التلقائي لجلسات الفريلانسرز (cron) لسا ما انتجرب فعلياً بجلسة منتهية بوقت حقيقي** — الآلية مبنية ومنشورة (GitHub Actions كل 10 دقائق)، بس ما في تأكيد ميداني حتى الآن.

## وحدة الموظفين (Staff) — جديدة بالكامل

### 1. السبب

كان في ثغرة أمنية حقيقية: المدرب المستقل (Freelancer) يقدر يمسح رموز QR جلساته بنفسه، وهو نفسه يلي بيتحدد عليه المبلغ يلي بيدفعه حسب نتيجة المسح (سعر حضور فعلي أقل من غرامة غياب) — يعني عنده مصلحة مباشرة يمسح رموز حتى لو الطفل ما إجا فعلياً، مشان يدفع أقل. الحل: نقل صلاحية المسح لموظف مستقل (النادي)، مو المدرب نفسه.

بالتوازي، تقرر توحيد دخول الموظفين: بدل ما يضل `/attendance` (النظام الأساسي) عام بالكامل بينما مسح الفريلانسرز محمي، صار الاثنين تحت نفس بوابة دخول الموظف الواحدة — قرار واعي بتغيير فلسفة "الحضور عام" الأصلية، بعد ما اتناقش وتأكد صراحة.

### 2. البنية

- جدول `staff_settings` (كلمة مرور مشتركة واحدة، نفس نمط `admin_settings` تماماً، صف واحد بمفتاح ثابت).
- كوكي جلسة `bwa_staff_session` (نفس نمط `bwa_admin_session` — مقارنة نصية مباشرة، مش hash، لنفس سبب Edge Runtime).
- `middleware.js`: حماية `/staff/*` بالكامل (عدا `/staff/login` نفسها)، وحماية `/attendance` بالكامل (الصفحة + `POST /api/attendance`، بينما `GET /api/attendance` ضل عام لأنه `/history` بتعتمد عليه).
- كلمة مرور الموظفين تدار من `/admin` فقط (زر "🔑 كلمة مرور الموظف")، بدون تحقق من كلمة مرور حالية (نفس منطق تصفير PIN الفريلانسر — الأدمن مصادق أصلاً).

### 3. الصفحات

| المسار | الوصف |
|---|---|
| `/staff/login` | فورم دخول بكلمة مرور واحدة |
| `/staff` | بوابة بعد الدخول: زرين ("تفقد الأكاديمية" → `/attendance`، "تفقد الفريلانسرز" → `/staff/freelancer-scan`) + خروج |
| `/staff/freelancer-scan` | قائمة جلسات الفريلانسرز الجاهزة للمسح (`approved`/`checked_in`) |
| `/staff/freelancer-scan/[id]` | صفحة مسح فعلية لجلسة معيّنة — نفس منطق الكاميرا/الأصوات/الـcooldown المستخدم بـ`/attendance` تماماً، بس عبر endpoint إداري منفصل |

### 4. الأثر على وحدة Freelancers

- `POST /api/freelancer/sessions/[id]/scan` **معطّل نهائياً** (يرجّع 403 دايماً) — المدرب ما عاد يقدر يمسح لنفسه بأي شكل.
- صفحة `/freelancer/sessions/[id]/scan` **محذوفة بالكامل** من الكود.
- صفحة تفاصيل جلسة المدرب (`/freelancer/sessions/[id]`) فقدت زر "الانتقال للمسح"، بس احتفظت بكل شي تاني (توليد QR، تحميل صورة، عرض حالة الرموز، إغلاق يدوي).
- endpoint جديد `POST /api/admin/freelancer-sessions/[id]/scan` — نفس منطق `scan_freelancer_qr_token()` بالضبط، بس محمي بكوكي الموظف عبر middleware (مش كوكي المدرب).

## وحدة Freelancers (المدربون المستقلون) — محدّثة، الآن كاملة UI+Backend

### 1. نظرة عامة

الفريلانسرز مدربون مستقلون (مش موظفين بالأكاديمية) بيستأجروا وقت مسبح لحصصهم الخاصة. الدورة الكاملة: **حجز موعد ← موافقة/رفض من الإدارة (بتفاصيل الأعداد والتكلفة المتوقعة) ← تسجيل حضور الأطفال بمسح QR من جهة الموظف ← إغلاق الجلسة (يدوي أو تلقائي) ← محاسبة تلقائية بالـledger ← عرض/دفعات/عكس حركات من جهة الإدارة**.

### 2. الجداول

(بدون تغيير عن السابق) — `freelancers`، `level_default_pricing`، `freelancer_pricing_overrides`، `freelancer_sessions`، `freelancer_session_level_counts`، `session_qr_tokens`، `freelancer_ledger_entries`، `freelancer_payment_type_history`، `freelancer_settings`، `freelancer_notifications`، View: `freelancer_balances`.

### 3. دوال قاعدة البيانات (SQL Functions)

(بدون تغيير) — `reset_freelancer_test_data()`، `close_freelancer_session()`، `create_freelancer_session_request()`، `scan_freelancer_qr_token()`، `add_freelancer_ledger_entry()`.

### 4. الـAPI Endpoints الفعلية

**المصادقة** — بدون تغيير (`/api/freelancer/login`, `/logout`, `/me`).

**واجهة المدرب** — كل الـendpoints زي ما كانت، **عدا**: `POST /api/freelancer/sessions/[id]/scan` معطّل نهائياً (403 دايماً، لا يستقبل أي منطق مسح بعد اليوم).

**واجهة الإدارة** — كل الـendpoints القديمة زي ما هي، **بالإضافة**:
| Method | Path | الوصف |
|---|---|---|
| GET | `/api/admin/freelancer-sessions/[id]` | تفاصيل جلسة (session + levelCounts + qrTokens) من منظور الإدارة، بدون شرط ملكية |
| POST | `/api/admin/freelancer-sessions/[id]/scan` | تسجيل مسح QR من جهة الموظف (بديل مسار المدرب المعطّل) |
| GET | `/api/admin/freelancers/[id]/ledger` | سجل حركات مالية كامل لمدرب معيّن (جديد — كان الـPOST بس موجود) |

`GET /api/admin/freelancer-sessions` صار يدعم فلترة بأكتر من حالة سوا (`?status=approved,checked_in`، مفصولة بفاصلة)، بالإضافة للفلترة بحالة وحدة القديمة (توافق عكسي كامل).

### 5. آلية المصادقة (المدرب) — بدون تغيير

### 6. آلية الإغلاق التلقائي — بدون تغيير (لسا ما انتجربت فعلياً بجلسة حقيقية منتهية)

### 7. ملاحظات فنية مهمة — بدون تغيير (تحذير `lib/db.js` / `fetchOptions: { cache: 'no-store' }` لسا ساري)

### 8. حالة الواجهات (UI) — محدّثة بالكامل، الوحدة مكتملة الآن

**واجهة المدرب** (`/freelancer/*`, خلف دخول رقم جوال + PIN):
- `/freelancer/login` — دخول
- `/freelancer` — لوحة رئيسية (الاسم، الرصيد، عدد الجلسات المعلّقة)
- `/freelancer/book` — حجز جلسة جديدة
- `/freelancer/sessions` — قائمة جلساته بكل الحالات، فلترة بالحالة
- `/freelancer/sessions/[id]` — تفاصيل جلسة: الأعداد المطلوبة، توليد/تحميل QR (بدون مسح — عرض حالة فقط)، إغلاق يدوي
- `/freelancer/ledger` — كشف حساب كامل (الحركات + الرصيد الحالي)
- `/freelancer/notifications` — إشعارات (موافقة/رفض/إغلاق)، تعليم كمقروء

**واجهة الموظف** (`/staff/*`, خلف كلمة مرور الموظفين المشتركة):
- `/staff/login`, `/staff` (بوابة)
- `/staff/freelancer-scan` — قائمة الجلسات الجاهزة للمسح
- `/staff/freelancer-scan/[id]` — كاميرا مسح فعلية (نفس منطق `/attendance`)

**واجهة الإدارة** (`/admin`, `/admin/freelancers`, خلف كلمة مرور الإدارة):
- قسم "طلبات الفريلانسرز" بالصفحة الرئيسية — موافقة/رفض بتفاصيل الأعداد والتكلفة المتوقعة لكل مستوى
- `/admin/freelancers` — قائمة المدربين (إضافة جديد، تفعيل/تعطيل، نوع الدفع، تصفير PIN، أسعار خاصة لكل مستوى)، السجل المالي الكامل لكل مدرب (عرض، تسجيل دفعة باتجاه صريح وارد/صادر، عكس أي حركة)، والأسعار الافتراضية للمستويات

---

## شو بدي من Claude أثناء النقاش

لا أريد كتابة كود بهالمحادثة. أريد منك:

- نقد الفكرة يلي رح أطرحها بصراحة.
- اقتراح مشاكل مستقبلية ممكن تصير.
- مقارنة بدائل مختلفة للحل.
- التفكير بتجربة المستخدم (خصوصاً للموظف غير التقني وبالنادي المزعج/الصاخب).
- اقتراح أفضل workflow لأي ميزة جديدة.
- التنبيه إذا كان اقتراح معيّن رح يأثر على الأداء أو قاعدة البيانات.
- **ما تقترح تعديلات تغيّر فلسفة النظام** (القسم أعلاه "أشياء أساسية ما لازم تتغيّر") **إلا إذا سألتك عنها صراحة**.

لما نوصل لقرار واضح، بارجع فيه لمحادثة Claude Code للتنفيذ الفعلي بالكود.
